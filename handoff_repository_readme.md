# 交接文档：`repository_readme`

> 本文面向完全没有本次会话上下文的新会话。
> 流水记录见 `worklog_repository_readme.md`。

---

## 1. 我们在做什么任务

工作名为 `repository_readme`。任务是为 `Histogram-simulation` 仓库维护根目录 `README.md`，
让内部仓库与 GitHub 镜像显示同一份项目说明。

README 的目标读者是第一次接触项目的开发者或研究人员，需要让读者在不翻遍根目录的情况下知道：

- 仓库研究什么；
- 数据如何从光链路流向二值直方图和检测判决；
- 各工作线当前应打开哪个 notebook；
- 哪些命令只重建分析，哪些命令会触发耗时扫描；
- 缓存、并行、验证和版本演进有哪些约束。

## 2. 已经完成了什么

- 新建 `README.md`；
- 写入项目总览和文本矢量数据流示意图；
- 定义 README 中使用的专业缩写及其英文全称和中文含义；
- 索引七个当前入口及六份主要 handoff；
- 写入基础 Python 环境安装命令；
- 写入 `peak_vs_energy`、`PoD_esti_v30`、两个 peak 专题的重建与校验命令；
- 写入长扫描缓存、默认 20 进程、增量检查点和原子保存规则；
- 写入专题内核版本不能随意替换、v42 无 builder、v45 部分手工演进等限制；
- 明确仓库当前没有 `LICENSE` 和依赖锁文件。
- README 的 23 个相对链接和 2 张预览图均已确认存在；基础依赖导入、路径泄漏、标题层级和
  Git 空白检查均已通过。

没有修改任何仿真代码、notebook、缓存、图片或物理参数。

## 3. 目前面临的问题、卡在哪里

当前无阻塞。

仍有两个仓库级缺口，但不属于本次“写 README”的授权范围：

1. 没有 `requirements.txt`、`pyproject.toml` 或环境锁文件，依赖版本不能精确复现；
2. 没有 `LICENSE` 文件，外部使用和再分发授权范围不明确。

## 4. 下一步计划

1. 各工作线当前入口、运行命令或缓存规则变化时，同步更新 `README.md`；
2. 新增工作线时，补充推荐入口、handoff 链接和最小运行命令；
3. 后续若用户授权，可单独补依赖锁文件或许可证；
4. README 修改后继续保持内部远端与 GitHub 镜像内容一致。

## 5. 踩过的坑（不要再踩）

- 不要只按 notebook 文件名大小或版本号判断推荐入口，必须先读对应 handoff / worklog。
- 不要把所有专题统一改为 `pod_esti_v30_core.py`；`peak_vs_noise` 和 `peak_distribution` 当前依赖 v05 内核。
- 不要在 README 中给出内部远端地址；两个镜像共用内容即可。
- 不要把已有缓存描述成原始样本；很多缓存保存的是 `bincount`、矩或临界点等充分统计量。
- 不要承诺“安装任意最新依赖就能逐位复现”；在依赖未锁定前只能说明基础包和已知差异风险。
- 不要使用 v41 builder 覆盖手工演进的 `crosstalk_sim_v42.ipynb`。
