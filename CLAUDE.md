# 项目规则（务必遵守）

## 交流语言
- **所有回答一律使用中文。** 包括正文、解释、总结、代码注释。

## 缩写
- **第一次出现的缩写必须给出英文全称并解释含义**，之后可直接用缩写。
  - 例：TCSPC（Time-Correlated Single Photon Counting，时间相关单光子计数）、
    SPAD（Single-Photon Avalanche Diode，单光子雪崩二极管）、
    IRF（Instrument Response Function，仪器响应函数）、
    ToF（Time of Flight，飞行时间）、PDE（Photon Detection Efficiency，光子探测效率）、
    BRDF（Bidirectional Reflectance Distribution Function，双向反射分布函数）。

## 代码修改
- **不得擅自改动用户已设定的参数值**（如目标距离、反射率、脉冲参数、bin 宽等）。
  只修改用户明确要求修改的部分。
- 若某项改动会连带影响其他参数，先说明、征得同意，再动。
- 用户说"画到某距离"指的是**绘图坐标范围**，不等于修改物理场景（目标距离等）参数。

## 文档维护与仿真工程规则（必须遵守）

### 规则一：会话交接文档 `handoff_<工作名>.md`
- **每次会话结束前必须写入或更新** 仓库根目录的 `handoff_<工作名>.md`。
- **工作名与 notebook / 工作标识一致**（例：`PoD_esti` → `handoff_PoD_esti.md`）。
- **禁止**写成 `handoff_现在工作.md`；不要把「现在工作」四个汉字放进文件名。
- 不存在则新建，已存在则修改补充。
- 读者假设：**一个完全没有上下文的新会话**，不得使用只有本次会话才懂的指代。
- 必含小节：① 我们在做什么任务 ② 已经完成了什么 ③ 目前面临的问题、卡在哪里
  ④ 下一步计划 ⑤ 踩过的坑（不要再踩）。

### 规则二：工作日志 `worklog_<工作名>.md`
- **从会话开始就要建立并持续维护**，工作名由用户给定（例：`PoD_esti` → `worklog_PoD_esti.md`）。
- 分两部分：**顶部**记录现状与当前任务（可覆盖更新）；**下部**为历史记录（只追加），
  逐版本写清新增、删减、参数改动、踩过的坑、关键结论。

### 规则三：耗时仿真必须缓存 + 多线程（自 `PoD_esti` v05 起）
- 预计超过约 1 分钟、或重跑代价很高的 MC / 批量扫描结果，必须落盘缓存；长扫描要有增量检查点与断点续跑。
- 默认并行线程数 `N_WORKERS = 20`（本机 CPU 支持 20 线程）；提高线程数时同步减小 `MC_CHUNK`。
- 细节见 `.cursor/rules/pod-cache-parallel.mdc`。

## 本对话已授权
- 本对话中所有代码修改默认同意，无需逐条确认。
