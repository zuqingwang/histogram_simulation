# PoD_esti v30 —— 理论与公式推导

本文件收走了 `PoD_esti_v30.ipynb` 里全部的理论说明与公式推导。notebook 只保留
"这个模块在算什么" 的基本描述与缩写；想知道 "为什么曲线长这样" 就看这里。

配套的两份专题文档仍然有效，本文只做索引与补充：

- `theory_peak_bg_multishot.md` —— 多发累加下 peak 与阈值的完整推导
- `theory_engine_equivalence.md` —— 三个 SPAD 引擎为什么等价

历史踩坑记录在 `worklog_PoD_esti.md`；跨会话交接看 `handoff_PoD_esti.md`。

---

## 0. 记号与口径

| 记号 | 定义 |
|---|---|
| `n_pix` | 宏像元里的 SPAD 数（3×9 = 27） |
| `N_shots` | 累加发数 |
| `n_tr` | 轨迹数 `= n_pix × N_shots` |
| `p_eq` | 单 SPAD、单发、单个 1 ns bin 的**平衡态点亮概率** |
| **`bg`** | `hist_add` 统计窗内每 bin 的平均计数，`bg = n_tr · p_eq`。**全项目唯一横轴** |
| `noise` | 单发 `hist_i` 每 bin 的平均计数，`noise = bg / N_shots` |
| `peak` | 一条 `hist_add` 在统计窗 152 个 bin 内的最大计数 |
| `T` | 检测阈值，**整数计数** |
| `T_OVER` | 过阈窗，约 8 ns |
| `τ_RC` | 过偏压恢复时间常数 |

**三个必须先说清的口径**，引用任何"差几倍"的数字时都要注明用的是哪一种：

1. **同天光 vs 同 bg**（模块 15）：同一片天光下 `n_tr` 小的配置 `bg` 天然更低、占便宜。
   拉到同一个 `bg` 才是公平的探测能力比较，但这意味着两个配置处在不同强度的环境光里。
2. **信号均匀 vs 按像斑加权**（模块 15）：按像斑
   `FX = [0.0014, 0.0152, 0.084, 0.234, 0.330, …]` 加权，9 列砍到 6 列只丢 1.8% 信号，
   → 3×6 更灵敏；按均匀处理时 3×6 只收到 2/3 信号 → 结论反转成 3×6 差约 1.2 倍。
   **两个结论都对，只是口径不同。本仓库统一采用「均匀」口径**（用户指定：噪声与信号在
   SPAD 上都是均匀的）。
3. **净峰高 `S_net` vs 净增量 `ΔS`**（模块 12）：即使完全没有信号，在 15 个 bin 的信号窗里
   取最大值也已经高于 `bg`（极值抬升约 2σ），所以 `S_net = peak − bg` 会**高估**信号贡献。

---

## 1. 为什么阈值 T 只能是整数（v30 新增，务必先读）

`hist_add` 是 `n_tr = 27 × N_shots` 条**二值**（0/1）轨迹逐 bin 求和，所以它的取值只能是
整数 `0 … n_tr`。判定规则就是一行：

```python
pod = (peak >= T).mean()
```

`peak` 是整数，因此 `peak >= 10.25` 与 `peak >= 11` 是**完全相同的判定**。
在"累加计数"这根轴上，0.25 粒度的阈值不存在物理意义，硬件也无法实现。

**阈值曲线为什么看起来像阶梯**：`bg` 是**均值**，可以连续地以 0.25 步进；
`T` 是**单次实现上的判定门限**，只能按 1 计数跳。48 个 bg 点里往往好几个共用同一个 `T`，
连起来自然是台阶。**这不是画法问题，也不是 bug。**

唯一能让阈值轴出现 0.25 粒度的真实口径是**除以 `N_shots` 看"每发平均计数"**，
此时粒度 `= 1/N`，`N=4` 恰好是 0.25。但它与 `bg`（累加口径）不同轴，混画会串口径，
所以 v30 不采用，统一保持整数计数轴。

---

## 2. 单 bin 是二项分布，不是泊松

SPAD 是 1 bit 器件：一个 bin 在一发里最多点亮一次。`N` 发累加后每个 bin 服从

$$X \sim \mathrm{Bin}(n_{tr},\, p_{eq}),\qquad n_{tr}=27N,\qquad \mathrm{bg}=n_{tr}p_{eq}$$

于是

$$\sigma^2 = \mathrm{bg}\left(1-\frac{\mathrm{bg}}{n_{tr}}\right),
\qquad F=\frac{\sigma^2}{\mu}=1-\frac{\mathrm{bg}}{n_{tr}}<1$$

这就是**亚泊松**（Fano 因子 `F < 1`）。这条公式是模块 10 第 ① 张图的解析对照线。

**关键推论**：同一个 `bg` 下，`N` 越大 `n_tr` 越大，方差压缩越弱、`σ` 越大，
因此阈值 `T ≈ μ + zσ` 也越高。这直接解释了模块 9 里 `ρ_N = T_N / T_1 > 1`。

### 2.1 一个反直觉但重要的结论

曾经的直觉是：「`N=4`、每发 `noise=1` 时，各发的峰不会落在同一个 bin，
所以叠加后的 peak 应该比 `N=1`、`noise=4` 时更小」。

**这是错的。** `check_same_bg_two_ways.py` 的定向 MC 表明，**同 `bg` 下 `N` 大的那一边 peak 更大**。
原因就是上面的方差公式：`N=1` 时 `n_tr=27`，`p_eq` 被逼到很高，方差被压得很狠；
`N=4` 时 `n_tr=108`，同样的 `bg` 只需要 1/4 的 `p_eq`，压缩弱得多。

"峰不对齐"确实存在，但它影响的是**信号**（相干累加），对**噪声底**的极值统计不起主导作用。

---

## 3. peak 是 152 个 bin 的极大值：极值统计与有效独立 bin 数

### 3.1 bin 之间是正相关，不是负相关

过阈窗 `T_OVER ≈ 8 ns` 让一次雪崩同时点亮相邻约 8 个采样点，造成**强正相关**。
`check_bin_correlation.py` 实测自相关函数（ACF）在 lag 1–7 都显著为正。
因此有效独立 bin 数 `M_eff` 远小于名义的 152。

> **更正记录**：早期文档写成"死时间造成相邻 bin 负相关"，方向是反的。实测是**正相关**。
> 这条已经在 v11 期间更正，此处再记一次，防止重犯。

### 3.2 Gumbel 极值近似（模块 10 第 ③ 张图的解析线）

设单 bin 标准差为 `σ_bin`（第 2 节公式），peak 相对 bg 的抬升量记为

$$z_M=\frac{\mu_{peak}-\mathrm{bg}}{\sigma_{bin}}$$

在 Gumbel 极限下，`M` 个近似独立同分布高斯变量的极大值，其标准差为

$$\sigma_{peak}\;\approx\;\frac{\pi}{\sqrt6}\cdot\frac{\sigma_{bin}}{z_M}$$

notebook 模块 10 把 `z_M` 用**实测的** `μ_peak` 反算出来（而不是用 `√(2 ln M)`），
这样就把"有效独立 bin 数被相关性压低"这件事自动吸收进去了，
不必显式估计 `M_eff`。实测曲线与这条解析线贴合得相当好。

### 3.3 三层阈值模型（精度递增）

1. **高斯极值近似**：`T ≈ bg + z·σ_bin`，最粗，只适合看趋势。
2. **大偏差（LD）近似**：在关心的 FAR 范围（10 ppm – 10%）内已经足够，
   细节见 `theory_peak_bg_multishot.md` 第 5–8 节。
3. **精确二项分位**：`far_threshold_binom_indep()`，假设 bin 独立，是**保守对照**
   （因为忽略了正相关，会高估有效 bin 数，从而高估阈值）。

MC 实测（`far_threshold_from_cnt`）永远是准绳，上面三个只是解释框架。

### 3.4 阈值比值 ρ 不是常数

$$T\;\approx\;a\,\mathrm{bg}+z\sqrt{b\,\mathrm{bg}}$$

`√bg` 项的权重随 `bg` 变化，所以 `ρ_{N/1} = T_N / T_1` **不是常数**；
只有 `bg` 很大时 `ρ` 才趋于 `a_N / a_1`。模块 9 的残差图量化了这一点。
小 `bg` 处 `T` 只有几个计数，整数量化噪声极大，所以模块 9 统计 `ρ̄` 时默认忽略 `bg < 1` 的档。

---

## 4. 有效 z 值（模块 11）

$$z=\frac{T-\mu_{peak}}{\sigma_{peak}}$$

分母是**噪声 peak 的标准差**，不是单 bin 的。它回答"这条 FAR 的阈值站在噪声 peak 分布的第几个 σ"，
是把不同 `bg`、不同 `N` 拉到同一把尺子上比较阈值裕量的最直接方式。

如果 peak 分布严格服从 Gumbel，`z` 应当只由 FAR 决定、与 `bg` 和 `N` 无关：

$$z \;=\; \frac{-\ln(-\ln(1-\mathrm{FAR})) - \gamma}{\pi/\sqrt6}$$

（`γ ≈ 0.5772` 是 Euler–Mascheroni 常数。）实测 `z` 随 `bg` 的漂移量，就是
"二项 + 正相关 + 整数量化"三者对纯 Gumbel 的偏离程度。

---

## 5. 同一信号强度、不同 bg 时 peak 怎么变（模块 14）

### 5.1 "peak 均值就是信号峰加上 bg" 这个假设不成立

理想加法假设是

$$\mu_{ideal}(\mathrm{bg})=\mu(b,\mathrm{bg}_{min})+(\mathrm{bg}-\mathrm{bg}_{min})$$

即斜率为 1 的直线。模块 14 第 ② 张图把它画成黑虚线。**实测曲线落在这条线下方，斜率 < 1。**

### 5.2 抢占（preemption）模型：为什么斜率小于 1

根因还是 1 bit。设某个 SPAD 在信号 bin 上因**环境光**点亮的概率是 `p_eq`，
信号单独作用时点亮概率是 `q`。因为一个 bin 最多记 1 次，两者不能相加，只能取并集：

$$p_{\text{both}} = p_{eq} + q - p_{eq}q$$

于是信号带来的**净增量**是

$$\Delta = p_{\text{both}} - p_{eq} = q\,(1-p_{eq})$$

`bg` 越高 → `p_eq` 越大 → `(1 − p_eq)` 越小 → **同样一份信号被吃掉得越多**。
这就是模块 8 下排"归一化增益 `Δpeak / boost` 随 bg 下滑"的直接原因，
也是模块 14 加法假设斜率小于 1 的原因。

注意这个效应对 `N` 敏感：同一个 `bg` 下，`N` 越大 `p_eq = bg / n_tr` 越小，
抢占越轻，所以**多发累加不仅抬高阈值，也更能保住信号**。两个效应方向相反，
最终谁赢要看模块 12 的所需信号曲线。

### 5.3 peak 标准差怎么变（**别凭直觉，实测否掉了直觉**）

一个很自然的猜测是："有信号时 peak 被钉在信号 bin 上，'从 152 个 bin 里挑最大'的极值涨落
被削弱，所以 `σ_peak` 应该变小。"

**这个猜测是错的。** v20 期间用 8 000 MC × 1296 档实测过：`σ(b) / σ(0)` **始终 ≥ 1**。
原因是信号自身也是二项过程，它带来的涨落叠加在噪声之上；
"钉住位置"省下来的极值离散度，抵不过多出来的信号涨落。

模块 14 第 ③ 张图就是把这两条摆在一起，让这件事一眼可见。**引用时不要写反。**

另外，peak 的**偏度**变化很大：纯噪声下 peak 分布明显右偏（极值分布的典型特征），
加信号后偏度掉到约 0.05，接近对称的二项形状。
所以只报"均值 + 标准差"不足以描述 peak，必须看完整分布——这就是模块 14 第 ① 张图存在的理由。

---

## 6. 三个引擎为什么等价（模块 3）

用户曾担心 PoD 把 SPAD 简化成了"8 ns 硬死时间"。**并没有。**
`check_engine_vs_v45.py` 做过三级核对：源码逐行、同种子比特级（60/60 条轨迹逐 bin 完全相同）、统计级。

RC 恢复是显式建模的：过偏压占比 $V_{ov}=1-e^{-\Delta t/\tau_{RC}}$，
触发概率 $\mathrm{PDE}_{max}\cdot g(V_{ov})$，每次雪崩把 $V_{ov}$ 打回 0 重新恢复。

等价链条（完整版见 `theory_engine_equivalence.md`）：

1. **泊松稀释**：入射光子是泊松流，探测是独立稀释 → 探测事件仍是泊松流，强度 $r_{\det}$。
2. **更新过程**：每次雪崩把器件状态复位，下一次雪崩只依赖"距上次雪崩多久" → 更新过程，
   风险函数 $h(t)=r_{\det}\,g\!\left(1-e^{-t/\tau_{RC}}\right)$。
3. **逆变换抽样**：间隔分布 $F(t)=1-e^{-\int_0^t h}$ 可预先做成 $H^{-1}$ 查找表，$O(1)$ 抽样。
4. **并集恒等式**：每次雪崩点亮一段长 $T_{OVER}$ 的窗；这些窗的并集用差分数组 $O(n)$ 求出，
   与逐点判断等价。
5. **更新-回报定理**：平衡态下 $p_{bin}=T_{OVER}/\mathbb{E}[X]$ 的连续时间形式，给出解析 $p_{eq}$。
6. **$n_{tr}$ 折叠**：各 SPAD、各 shot 独立同分布，纯噪声下只以 $n_{tr}=n_{pix}\times N_{shots}$ 出现。
   模块 15 的等 `n_tr` 校验就是这条的实证：3×6@N=6 与 3×9@N=4 的纯噪声阈值应当**完全相同**。
7. **离散化误差**：逐光子引擎用 $dt$ 步进，误差是 $O(dt^2)$；实测 $dt$ 减半误差降到约 1/4
   （比值 3.89 / 3.97 / 3.99 / 4.00，干净的二阶收敛）。

### 6.1 未了结的疑点（不要忘）

`theory_engine_equivalence.py` 的 T5b 里，步进引擎 `binary_macro_stepping` 在
$dt = 800/200$ ps 两档都比解析值**偏低**（2.2σ / 0.7σ），怀疑是"先出 bin、再处理本步雪崩"
的半步对齐问题。

- 纯噪声扫描（模块 5）用的是快速引擎 A，**不受影响**。
- 但 **PoD 信号支路（模块 6）用的正是这个步进引擎**，建议加大样本复核。

---

## 7. 平方反比测距（模块 13）

回波功率 ∝ `1/D²`，再叠大气衰减 `exp(−2αD)`。给定发射能量预算 `E_bud`，
可用能量倍率 `boost_avail = E_bud / E_PULSE_BASE`；模块 6 给出临界倍率 `boost_crit`。
需要的接收端衰减比

$$\mathrm{ratio}=\frac{\mathrm{boost}_{crit}}{\mathrm{boost}_{avail}}$$

解下面这个方程即得可测距离：

$$\left(\frac{D_{ref}}{D}\right)^{2}e^{-2\alpha (D-D_{ref})}=\mathrm{ratio}$$

`equiv_distance()` 就是在解它（对 `D` 单调，用查表插值）。
纯平方反比口径（`α = 0`）的闭式解是 `D = D_ref / √ratio`，模块 13 两张图分别对应这两种口径。
