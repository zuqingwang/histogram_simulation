---
description: 统一本项目发光时序、tcode 与回波滤除图的绘图规范
globs: "**/*.{py,ipynb}"
alwaysApply: true
---

# 直方图仿真绘图规范

- 发光时序统一采用 `crosstalk_sim_v23.ipynb` cell 8 的 kick 栅格：横轴 kick、纵轴激光器、方块填充、格内写编码值。
- tcode 码表统一采用 `crosstalk_sim_v220.ipynb` 原 cell 34 的矩阵风格：kick × 激光器矩阵、格内数字、色条。
- 回波图统一采用 `crosstalk_sim_v23.ipynb` cell 12 的逐回波堆叠柱风格：每条回波单独成柱，同距离上下堆叠，柱内标来源激光器和 kick。
- 滤除结果保留 `crosstalk_sim_v23.ipynb` cell 28 的清晰标记：黑叉表示被滤除；红色空心圈仅表示误杀。
- 真回波、模组内串扰鬼影、雷达对射必须用清晰且全项目一致的类型标识；雷达对射固定红色/斜线纹理，不能只靠竖虚线区分。
- 每张含对射的回波图必须同时画真实信号，并标出二者是否落在同一峰宽范围。
- 公式块使用 `$$...$$`，不要使用当前 Markdown 渲染器不能识别的 `\[...\]`。
- 图题、图例和坐标必须说明物理量、单位、滤前/滤后状态与符号含义。
