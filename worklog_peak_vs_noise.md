# worklog_peak_vs_noise

工作名：`peak_vs_noise`
主产物：`peak_vs_noise_v01.ipynb`（分析绘图）+ `peak_vs_noise_scan.py`（多进程扫描）

---

## 顶部：现状 / 当前任务

### 当前版本
**v02**（主产物 `peak_vs_noise_v02.ipynb`）。v01 保留作对照。

### 这个工作要回答的问题
**给定固定的信号强度，环境噪声逐渐增强时，信号峰值（peak）的分布如何变化？是线性变化吗？**

### 口径（v02 起全项目统一）
| 符号 | 含义 |
|---|---|
| **noise** | 环境噪声标准：折合到 **N_shots=1、宏像元 27 SPAD、每 1 ns bin** 的平衡态底计数；**与发数无关** |
| **bg** | 当前 `N_shots` 下，统计窗内累加波形的实测 baseline 均值（= 缓存 `noise_mc`） |

约定：**N_shots=1 时 bg ≈ noise**；N_shots=4 时平衡态下 **bg ≈ 4·noise**（v02 实测中位比 = 1.00 / 4.00）。

### 当前可运行状态
**v02 已跑通（读缓存出图）。**
- `peak_vs_noise_scan.py`：多进程扫描，缓存仍为 `peak_vs_noise_v01_cache.npz`（65 KB）；**v02 不重扫，只改画图口径**。
- `build_peak_vs_noise_v02.py` → `peak_vs_noise_v02.ipynb`：凡原 noise 轴图都再画一张 bg 轴图。
- 已用 `python -m nbconvert --execute --inplace peak_vs_noise_v02.ipynb` 执行（约 47 s）。
- 计算内核直接 `import pod_esti_v05_core`，不复制物理参数。

### 已得结论（简版，详见 handoff）
**不是线性的，但要分两层说：**
1. 单条 `<peak>`–noise 曲线**看起来几乎是直线**（全程一次拟合 R² = 0.9966…1.0000），
   严格说是**次线性（饱和型）**，偏离低噪声切线最多 17.1% n_tr（纯噪声档）／2.2% n_tr（信号档，N=4）。
2. **真正的强非线性在"信号与噪声不可叠加"上**：
   - 噪声的边际贡献 `∂<peak>/∂noise` 随信号强度**下降 55%**（N=1 与 N=4 都是 55%）
     → 直接否定 `<peak> = f(信号) + g(noise)` 的可分离性；
   - 信号净增量 Δ 衰减到无噪声时的 **25%–50%**；
   - 可分辨度 d′ 最差只剩低噪声时的 **16%**。

### 当前生效的关键参数
| 参数 | 取值 | 说明 |
|---|---|---|
| `N_SHOTS_LIST` | `[1, 4]` | 两种累加发数都做（用户明确要求） |
| 二值硬上限 `n_tr` | 27 / 108 | = 27 × N_shots，peak 不可能超过此值 |
| `BOOST_LIST` | `[0, 0.004, 0.008, 0.016, 0.032]` | 固定信号强度档；`0` = 纯噪声基线 |
| 对应脉冲能量 | `0 / 3.20 / 6.40 / 12.79 / 25.58` nJ | = boost × `E_PULSE_BASE`（= 799.4 nJ） |
| noise 网格 | N=1：0.25→12.0 步长 0.25（48 档）<br>N=4：0.25→40.0 步长 0.25（160 档） | 直接取 `core.NOISE_GRID` |
| 额外参考档 | `noise = 0`（`r_amb = 0`） | 无噪声纯信号，用于可加性检验 |
| `N_MC` | 8000 条/（n_shots, noise, boost） | peak 取值域 ≤108，`bincount` 即充分统计量 |
| `MC_CHUNK` | 2000 | 控制单进程峰值内存 |
| `N_WORKERS` | 20 | `ProcessPoolExecutor` |

### 待办清单
- [x] 探测 boost → peak 的量级，挑出不饱和的信号档
- [x] 建 worklog
- [x] 写 `peak_vs_noise_scan.py`（多进程 + 缓存 + 检查点）
- [x] 跑全量扫描（实测 21.6 min，20 进程）
- [x] 写 `build_peak_vs_noise_v01.py` → `peak_vs_noise_v01.ipynb`，出图与线性性判定
- [x] 按实测结果**重写结论**（原先预设的"明显次线性"被数据推翻，见坑 8）
- [x] 写 `handoff_peak_vs_noise.md`
- [x] **v02**：按 noise/bg 约定双轴出图；`nbconvert` 执行通过
- [ ]（可选）扩展到多个目标距离 D，验证结论的普适性
- [ ]（可选）拟合 `<peak>(noise, boost)` 的饱和型经验闭式，供固件快速估算
- [ ]（可选）对少数关键 noise 档用 `--n-mc 200000` 加大尾部精度（与 10 ppm 阈值直接相关）

---

## 下部：历史记录（只追加，不删改）

### v02 — noise / bg 双轴出图（2026-08-08）

**新增**
- `build_peak_vs_noise_v02.py` → `peak_vs_noise_v02.ipynb`。
- 口径拆分：由 `E_lambda/r_det` 派生环境标准 `noise`；`noise_mc` 记为 `bg`。
- 所有原 noise 横轴图经 `AXIS_KINDS = [("noise",…), ("bg",…)]` 各画一张；切线/斜率按轴分开存于 `TANGENT_BY_AXIS`。

**未改**
- 扫描脚本与缓存键；仍读 `peak_vs_noise_v01_cache.npz`（无重扫）。
- 物理参数、boost 档、MC 条数。

**运行结论**
- N=1：中位 bg/noise = **1.00**（两轴几乎重合）。
- N=4：中位 bg/noise = **4.00**（横轴数值差约 4 倍；曲线形状随轴伸缩，物理结论不变）。
- `nbconvert --execute --inplace` 约 47 s 成功。

**踩过的坑**
- 生成器 `code(r"""...""")` 内不能再嵌 `"""` docstring → 内层用 `'''`。
- `TANGENT` 改名 `TANGENT_BY_AXIS` 时 `replace_all` 可能把名字弄坏，需手工核对。

### v01 — 本会话新建

**新增**
- `worklog_peak_vs_noise.md`（本文件）。
- `_probe_boost.py`（临时探测脚本，用完即删）：在 noise = 0.25 下扫 boost，测 peak 均值。

**关键探测结论（决定了信号档怎么选）**

信号响应在 27 / 108 的二值硬上限前**极早就饱和**，所以信号档必须取得很小：

noise = 0.25 时（400 条 MC）：

| boost | E [nJ] | peak 均值 (N=1，上限 27) | peak 均值 (N=4，上限 108) |
|---|---|---|---|
| 0 | 0 | 0.62 | 0.61 |
| 0.001 | 0.80 | 1.15 | 2.87 |
| 0.002 | 1.60 | 1.66 | 5.25 |
| **0.004** | **3.20** | **2.71** | **9.30** |
| **0.008** | **6.40** | **4.54** | **16.88** |
| **0.016** | **12.79** | **7.39** | **28.37** |
| **0.032** | **25.58** | **10.88** | **43.18** |
| 0.05 | 39.97 | 13.05 | 51.66 |
| 0.4 | 319.8 | 20.00 | 79.83 |
| 3.2 | 2558 | 24.72（≈上限 27） | 96.97（≈上限 108） |

- 最初试的 `boost = 0.05 … 3.2` 全部落在饱和区（N=1 时 peak 13→25），
  噪声的影响会被硬上限压掉，看不出规律 → **弃用**，改用 0.004…0.032（加粗行）。
- 选定的 4 个信号档在低噪声下 peak 约占硬上限的 10%–40%，
  且 N=1 与 N=4 的**相对占比一致**（10%/17%/27%/40% vs 9%/16%/26%/40%），
  所以两种 N_shots 用**同一组 boost**（同一绝对信号能量）即可直接对比。

**踩过的坑**
- 坑 1：一开始按 `PoD_esti` 的 `POD_LOG_BOOST_MIN/MAX` 直觉取 boost ≈ O(1)，
  结果全在饱和区。根因：`E_PULSE_BASE = 799.4 nJ`（≈0.8 μJ）是很强的脉冲，
  15 m / ρ=0.10 目标下 boost=0.05 就足以点亮 27 个 SPAD 的一半。
  正确做法：任何新的信号强度扫描**先探测再定档**，不要沿用别的模块的 boost 范围。
- 坑 2：`E_PULSE_BASE` 是 **799.4 nJ 而不是 1000 nJ**（`amp_mode="peak"`，由
  `P_peak=235 W` 与双指数 τ_r=0.7 / τ_f=1.9 ns 积分得出，不是参数表里的
  `E_pulse: 800e-9` 直接生效——数值接近纯属巧合）。
  第一版 `BOOST_LIST` 因此把探测表的「能量列 ÷ 1000」误当成了 boost，
  写成 `0.0032…0.0256`（实际 E = 2.56…20.47 nJ）。
  正确做法：boost ↔ 能量的换算一律写成 `boost * core.E_PULSE_BASE * 1e9` 现算，不要手推。

**速度实测（用于估算全量耗时）**
- N=1：约 8000 条 MC / 秒 / 单进程
- N=4：约 2100 条 MC / 秒 / 单进程（≈ N=1 的 1/4，与 n_tr 成正比，合理）
- 全量 = (48 + 160) 档 × 5 个 boost × 8000 条 ≈ 8.3 M 条
  → 单进程约 55 min，20 进程预估约 3 min。
- **更正**：20 进程实测 **21.6 min**，远慢于预估的 3 min。
  原因是任务粒度不均（N=4 的单档约 19 s，N=1 的单档约 5 s），
  且 20 个子进程各自 `import pod_esti_v05_core` 都要载噪声缓存 + 算六档 FAR 阈值。
  下次估算这类扫描要按"最重任务 × 任务数 / 进程数"再乘 1.5 的余量。

**新增（续）**
- `peak_vs_noise_scan.py`：多进程扫描脚本。
  - 任务粒度 = 1 个 (n_shots, noise 档)，该档下 5 个 boost 一次算完，共 210 个任务。
  - 缓存存 **peak 的 `bincount`** 而不是原始样本：peak 取值域 ≤108，
    `bincount` 即完整分布的充分统计量 → 全量缓存只有 **65 KB**，却能事后算任意分位数。
  - `k = K_NOFLOOR = -1` 是特殊档，表示 `r_amb = 0`（**noise = 0 的无噪声纯信号**），
    专门用于可加性检验的分母。
- `build_peak_vs_noise_v01.py` → `peak_vs_noise_v01.ipynb`（16 cell，9 图 + 3 表）。
- `figs_peak_vs_noise/`：9 张图的 png 留档。

**运行结论 / 关键数值（8000 条 MC/点）**

无噪声（noise = 0）时各信号档的 peak 均值：

| E [nJ] | N=1（上限 27） | N=4（上限 108） |
|---|---|---|
| 3.20 | 2.32 | 9.17 |
| 6.40 | 4.25 | 16.73 |
| 12.79 | 7.14 | 28.28 |
| 25.58 | 10.68 | 42.58 |

判据 1+2（曲线弯曲程度）：

| N | E [nJ] | k0 | k_end | k_end/k0 | 最大偏差 [%n_tr] | 全程线性 R² |
|---|---|---|---|---|---|---|
| 1 | 0（纯噪声） | 1.624 | 1.084 | 0.67 | −17.1 | 0.9966 |
| 1 | 3.20 | 1.327 | 1.028 | 0.78 | −9.4 | 0.9984 |
| 1 | 25.58 | 0.736 | 0.659 | 0.90 | −2.0 | 0.9997 |
| 4 | 0（纯噪声） | 1.385 | 1.050 | 0.76 | −9.2 | 0.9989 |
| 4 | 3.20 | 1.058 | 0.973 | 0.92 | −2.2 | 0.9999 |
| 4 | 25.58 | 0.619 | 0.598 | 0.97 | −0.1 | 1.0000 |

判据 3（可分离性）—— **最关键的一条**：低噪声斜率 `k0 = ∂<peak>/∂noise` 随信号强度单调下降

| 信号 E [nJ] | N=1 的 k0（相对无信号） | N=4 的 k0（相对无信号） |
|---|---|---|
| 0（无信号） | 1.624（100%） | 1.385（100%） |
| 3.20 | 1.327（82%） | 1.058（76%） |
| 6.40 | 1.133（70%） | 0.937（68%） |
| 12.79 | 0.915（56%） | 0.785（57%） |
| 25.58 | 0.736（**45%**） | 0.619（**45%**） |

→ 若 `<peak> = f(信号) + g(noise)` 可线性叠加，这一列必须是常数。
实测下降 55%（两种 N 完全一致），**可分离性被明确否定**。

信号净增量 Δ 的末端保留率（Δ 相对 noise=0 纯信号 peak）：

| E [nJ] | N=1（noise→12） | N=4（noise→40） |
|---|---|---|
| 3.20 | 25% | 34% |
| 6.40 | 26% | 40% |
| 12.79 | 31% | 45% |
| 25.58 | 36% | 50% |

可分辨度 d′ 的衰减（最低 noise → 最高 noise）：
- N=1，E=3.20 nJ：1.86 → 0.29（只剩 **16%**，最差）
- N=1，E=25.58 nJ：7.32 → 1.93（26%）
- N=4，E=3.20 nJ：4.39 → 0.78（18%）
- N=4，E=25.58 nJ：15.97 → 5.17（32%）

**踩过的坑（续）**

- 坑 3（生成器）：在 `build_*.py` 的 `code(r"""...""")` 内部写 `\"\"\"docstring\"\"\"`
  或嵌套 `print("""...""")`。
  现象：raw string 里 `\"` **保留反斜杠**，生成语法错误的 cell；嵌套 `"""` **提前终止**外层字符串。
  正确做法：生成的代码内部一律用**三单引号** `'''...'''`，并跑一遍逐 cell `compile()` 校验。
- 坑 4：notebook 的 `source` 用 `src.split("\n")`。
  现象：Jupyter 把所有行**拼成一行** → `SyntaxError: invalid syntax`
  （但自己写的逐 cell compile 校验脚本若自行补了换行，会**误判为通过**）。
  根因：nbformat 的 `source` 是「**每行自带换行符**」的列表（末行不带）。
  正确做法：`[ln + "\n" for ln in body.split("\n")[:-1]] + [body.split("\n")[-1]]`。
  顺带给每个 cell 加 `id` 字段，消除 `MissingIDFieldWarning`。
- 坑 5：在 `r"""..."""` 里给 matplotlib 标题写 `\\n` 想换行。
  现象：图标题里出现**字面的 "\n" 两个字符**。
  根因：r-string 中 `\\n` 是 3 个字符 → JSON → notebook 源码 `\\n` → Python 值是 `\`+`n`。
  正确做法：在 r-string 里就写单个 `\n`。
- 坑 6：图上用 `⟨peak⟩`（U+27E8/27E9 数学角括号）。
  现象：大量 `UserWarning: Glyph 10216 missing from font(s) Microsoft YaHei`，图上显示为方框。
  正确做法：改用 ASCII `<peak>`；数学模式 `$...$` 内只放拉丁字母（如 `$n_{tr}$`），中文放在外面。
- 坑 7：`jupyter nbconvert` 在本机 PowerShell 里**不在 PATH**。
  正确做法：用 `python -m nbconvert ...`（`python -m jupyter nbconvert` 也不行，会报
  `Jupyter command jupyter-nbconvert not found`）。
- **坑 8（最重要，方法论）：先写好结论文字，再拿数据去"验证"。**
  错误做法：在 notebook 的结论 cell 里预先写死了"⟨peak⟩ 随 noise 明显次线性、
  高噪声端斜率只剩低噪声端的一小部分"。
  现象：实测出来 **全程线性拟合 R² = 0.9966…1.0000**，信号档偏离线性外推最多只有 2.2% n_tr
  （N=4），预设的措辞与数据**明显不符**，等于把结论写反了强度。
  根因：把"机制上必然非线性"直接等同于"曲线形状上明显弯曲"。
  实际上二值硬上限造成的弯曲在本扫描范围内很弱，真正的强非线性表现在
  **可分离性**（斜率随信号强度变）与**信号净增量衰减**上，而不在单条曲线的曲率上。
  正确做法：结论文字里的**判定措辞与全部数值都要由实测自动生成**；
  非线性要分层论证（曲线曲率 / 可分离性 / 净增量），不能只看一条曲线直不直。
