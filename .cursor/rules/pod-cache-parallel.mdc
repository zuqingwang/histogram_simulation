---
description: PoD_esti 从 v05 起：耗时仿真必须落盘缓存，并默认多线程并行
alwaysApply: true
---

# 规则三：耗时仿真缓存 + 多线程（自 `PoD_esti` v05 起）

适用于本仓库中所有**耗时较长**的 Monte Carlo / 批量扫描仿真（尤其 `PoD_esti_v*.ipynb`）。

## 1. 必须落盘缓存

- 凡预计超过约 **1 分钟**、或需要重新跑很久才能复现的中间结果，**都必须写入磁盘缓存**。
- 缓存文件放在仓库根目录，命名清晰，例如：
  - `pod_esti_v05_cache_noise.npz`
  - `pod_esti_v05_cache_pod.npz`
- 缓存键必须包含决定结果的全部参数（网格、MC 条数、阈值目标、窗长等）。参数一变，不得误用旧缓存。
- **增量检查点**：长扫描不能只在全部结束后才存。至少每完成一档（或少量档）就写入 `.partial.npz`；中断后必须能断点续跑。
- 写缓存用「先写临时文件再 `os.replace`」，避免半截文件。
- 新版本可读取旧版兼容缓存，但一旦载入成功，应同步写到当前版本主缓存名，防止旧文件丢失后无法恢复。

## 2. 必须多线程优化

- 本机 CPU 支持 **20 线程**；默认 `N_WORKERS = 20`（或 `NOISE_WORKERS` / `POD_WORKERS` 同等设置）。
- 纯噪声分块、信号能量点评估等可并行部分，统一用 `ThreadPoolExecutor`。
- 提高线程数时必须同步减小 `MC_CHUNK`，控制峰值内存（20 线程时 `MC_CHUNK` 建议约 5000）。
- 不要假设线性加速；NumPy 受内存带宽/GIL 限制，但仍应默认启用多线程。

## 3. 改代码时的约束

- 不要为了“省事”删掉缓存读写。
- 不要把完整原始 peak 样本无必要地存成 GB 级文件；优先存充分统计量（如 `bincount`、临界点摘要）。
- 新增大规模扫描时，先设计缓存键与检查点，再写主循环。
