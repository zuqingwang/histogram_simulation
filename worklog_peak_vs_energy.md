# 工作日志：`peak_vs_energy`（信号能量扫描 —— peak 与半高全宽）

---

# 一、现状 / 当前任务

## 当前版本

**v01-e**（主口径 = `f_pix` 均匀照度；引擎已修饱和丢覆盖 bug），
主产物 `peak_vs_energy_v01.ipynb`（20 个 cell：markdown 8 / code 12）。

> ⚠️ 2026-08-10 修了 `pod_esti_v30_core.binary_macro_stepping*` 在**饱和**时丢过阈覆盖的 bug
> （详见下方 v01-e 条目）。**修复前产生的一切饱和区数值全部作废**，
> 包括 v01 / v01-u 条目里的封顶值、σ 拱顶幅度、FWHM 深饱和斜率。
> 低占空区（远未饱和）不受影响。

## 当前可运行状态

全部跑通。

| 环节 | 命令 | 耗时 |
|---|---|---|
| 蒙特卡洛扫描（离线，已完成） | `python -u run_peak_energy_scan.py --workers 20 --n-mc 20000` | 15.2 min（20 进程） |
| 重建 notebook | `python build_peak_vs_energy.py` | < 1 s |
| 无头校验（跑全部 code cell + 存图） | `python -u check_peak_vs_energy.py` | 约 5 s |

两份缓存都在，notebook 只读缓存，**打开即可全跑，不重算**：

| 缓存文件 | 照度模式 | 档数 | 大小 |
|---|---|---|---|
| `peak_vs_energy_cache_uniform.npz` | **当前主口径**，27 个 SPAD 平分 Σf_pix | 75 | 约 57 KB |
| `peak_vs_energy_cache_real.npz` | 对照，真实像斑轮廓（最大/最小差 241 倍） | 76 | 58 KB |

切换：改 `run_peak_energy_scan.py` 的 `F_PIX_MODE`，或临时 `set PVE_FPIX_MODE=real`。
notebook 读的是 `scan.CACHE`，跟着模式自动切。

## 当前生效的关键参数

| 参数 | 取值 | 位置 |
|---|---|---|
| `ENGINE_TAG` | **`"cov2"`**（覆盖判据修复版）。引擎再动必须升版本，否则旧缓存会被误用 | `run_peak_energy_scan.py` |
| `F_PIX_MODE` | **`"uniform"`**（27 个 SPAD 照度相同，Σf_pix 与真实像斑一致 = 0.05969） | `run_peak_energy_scan.py` |
| `BG_SIG` | `0.0`（无环境光） | `run_peak_energy_scan.py` |
| 能量网格（uniform） | 75 档，`boost` 从 0 到 1e4（非零最小 2.5e-4），**非均匀采样** | `build_boost_grid("uniform")` |
| 能量网格（real） | 76 档，端点相同，中段不同 | `build_boost_grid("real")` |
| 每档 MC | 20,000 | `--n-mc` |
| `N_SHOTS_MAX` / `N_SHOTS_LIST` | 4 / (1, 2, 4) | `run_peak_energy_scan.py` |
| `n_tr`（二值硬上限） | 27 / 54 / 108 | = 27 SPAD × N_shots |
| 统计窗 | bin `[24, 176)`，152 个 | 沿用 `pod_esti_v30_core` |
| `FWHM_MIN_PEAK` | 4 计数 | `run_peak_energy_scan.py` |
| `MC_CHUNK` / `CHECKPOINT_EVERY` | 2500 / 6 | `run_peak_energy_scan.py` |
| `T_OVER` | ≈ 8.00 ns（导出量） | `pod_esti_v30_core` |

## 待办

- [x] 探路：确认 bg=0 时 boost 需要多大才饱和、FWHM 是否会饱和
- [x] `fwhm_ns_batch` 向量化实现与朴素实现逐点对拍（含 6 种极端情形）
- [x] 76 档 × 20,000 MC 全量扫描 + 落盘缓存
- [x] 生成 notebook，五张指定图全部产出
- [x] 无头校验全部 cell 通过，字体无缺字
- [x] 主口径切到 `f_pix` 均匀照度并全量重算（见下方 v01-u 条目）
- [x] 修 `binary_macro_stepping*` 饱和丢覆盖 bug + 与精确引擎对拍（见下方 v01-e 条目）
- [ ] **用修好的引擎重算 `real` 口径缓存**（约 16 min），
      现有 `peak_vs_energy_cache_real.buggy.npz` 只能留档、不能引用
- [ ] **重跑 `check_uniform_vs_real_fpix.py` / `check_singlebin_vs_peak.py`**：
      它们的拱顶结论在低占空区应当不变，但「占比 1.000 处 σ_peak/σ_单bin = 0.175」
      这类深饱和数值是用旧引擎测的，必须复核
- [ ] **复核 PoD_esti v30 信号侧缓存**（`run_pod_v30_sig_scan.py` 用同一引擎）。
      近门限低占空区两套引擎一致，结论大概率不变，但要确认
- [ ] 在 Jupyter 里 Restart & Run All 确认渲染效果（缓存齐全，只是看排版）
- [ ] （可选）uniform 网格里 `boost ∈ [0.15, 0.5]` 采样偏疏（步长 0.05），
      模块 2 的密度条带在这一段格子偏宽、有块状感。
      若要更细，需改 `build_boost_grid()` 并**全量重算**（网格变了缓存键就失配）
- [ ] （可选）把 bg 开成非零列表，看环境光如何抬高 FWHM 地板 / 改变 σ 拱顶位置
- [ ] （可选）FWHM 深饱和段 4.40 ns/decade 与脉冲双指数 `τ_r=0.7 / τ_f=1.9 ns` 的解析关系尚未推导
- [x] **更正**：peak 标准差拱顶偏离 0.5 的成因已用对照实验判定 —— 是 `f_pix` 不均匀导致的
      泊松二项，不是「取最大值」。且拱顶应按区间（占比 0.2–0.4）引用，不可报三位有效数字。
      见下方 v01-c1、v01-c2 条目

---

# 二、历史记录（只追加，不删改）

## v01（2026-08-10）建立工作，完成能量扫描全流程

### 起因

用户指出 `lidar_histogram_sim_v45.ipynb` 的**模块 18**（信号强度倍数扫描，跨 7 个数量级、
用 colorbar 串起波形演化）很好，希望据此新建一个 notebook，把「看波形」推进到「看统计量」：
在 **bg = 0** 下扫描信号能量，考察 peak 的分布 / 均值 / 标准差，并要五张**线性坐标轴**的图：

- 图 i　peak 均值 vs 信号能量（最重要）
- 图 ii　peak 标准差 vs peak 均值
- 图 iii　peak 标准差 vs 能量
- 图 iv　半高全宽 vs 能量
- 图 v　半高全宽 vs peak 均值

### 开工前的设计确认（探路数据 → 提问 → 用户拍板）

先写临时探路脚本，用 300 次 MC 粗扫 `boost ∈ [1e-4, 1e4]`，拿到三条决定性事实：

1. peak 标准差**非单调**，在 peak 约占硬上限 30% 处见顶后回落；
2. FWHM 有**约 8 ns 的地板**（= `T_OVER`，一次雪崩点亮连续 8 个 bin）；
3. peak 在 `boost ≈ 20` 就彻底封顶，但 **FWHM 永不饱和**，每十倍能量稳定增宽约 4.7 ns。

据此向用户提三个问题并得到答复：

| 问题 | 用户答复 |
|---|---|
| 线性横轴铺到哪 | 要铺到**饱和之后更远**（为了看 FWHM 继续怎么变）；另外单独要「低能段线性检验」和「0→95% 饱和全局」两个视图 |
| 扫哪些 N_shots | 1、2、4 全要 |
| FWHM 在哪条曲线上量 | **N 发累加波形**上逐次实现测量，再对多次蒙卡求平均 |

**线性轴 vs 5 个数量级动态范围的矛盾，解法是「采样非均匀、坐标轴仍线性」**：
MC 点在低能段密、高能段疏，线性轴上曲线依然光滑，也不浪费机时。
用户此前「不用对数坐标轴」的要求是针对坐标轴，不针对采样。

### 新增文件

| 文件 | 作用 |
|---|---|
| `run_peak_energy_scan.py` | 多进程能量扫描 + 落盘缓存 + 增量检查点。物理内核**整个复用** `pod_esti_v30_core.py`，不定义任何新物理 |
| `build_peak_vs_energy.py` | 组装 notebook。改内容改这里再重跑，不要直接改 `.ipynb` |
| `check_peak_vs_energy.py` | 无头执行全部 code cell、每图存 PNG、核对实际字体 |
| `peak_vs_energy_v01.ipynb` | 主产物 |
| `peak_vs_energy_cache.npz` | 缓存，58 KB（v01-u 起改名为 `peak_vs_energy_cache_real.npz`） |

### 结构

沿用 PoD_esti_v30 的三段式：**模块说明（markdown）→ 画图参数 cell（每个参数注明控制哪根轴）→ 画图 cell（只读缓存）**。

| 模块 | 内容 |
|---|---|
| 0 | 载入缓存、派生统计量（peak 均值/标准差、FWHM 均值/标准差、平均波形、低能线性斜率、深饱和 FWHM 对数斜率） |
| 1 | 能量扫描总览。左＝平均累加波形（沿用 v45 模块 18 的 colorbar 画法）；右＝各自归一化到自身峰值，直观看出 FWHM 的来源 |
| 2 | peak 概率分布随能量变化的密度条带，3 列（N=1/2/4）× 2 行（低能段 / 全局） |
| 3 | **图 i**，三个横轴范围：低能段线性检验 / 全局 / 穿过饱和 |
| 4 | **图 ii + iii**，含二项参考曲线与拱顶位置表 |
| 5 | **图 iv + v**，含 `T_OVER` 地板线、深饱和对数拟合、有效样本不足区的灰色警示 |
| 6 | 全部能量档的数值汇总表（深饱和段线性轴画不下，定量以此表为准） |

### 缓存设计（遵循规则三）

落盘的是**充分统计量**而非原始样本，所以 76 档 × 3 个 N × 20,000 MC 只占 58 KB：

- `peak_cnt_{N}` (76, n_tr+1)：peak 的完整计数分布
- `fwhm_cnt_{N}` (76, 256)：FWHM 分布（0.25 ns 一格）
- `fwhm_sum / sumsq / nval_{N}`：FWHM 均值与标准差
- `wave_sum_{N}` (76, 200)：平均波形

缓存键 `GRID_KEY` 含 bg、N_shots_max、网格档数与端点、FWHM 门槛与分箱、统计窗，任一项变化即失配重算。
检查点每 6 档写一次 `.partial.npz`，先写临时文件再 `os.replace`。

**一次 4 发仿真通过前缀和白拿 N=1/2/4 三档**，所以 N_shots 维度几乎不额外花钱。

### 关键运行结论

扫描 76 档 × 20,000 MC，20 进程 15.7 min。

**① peak 均值 vs 能量：低能段严格正比，约在硬上限 10% 处离开线性**

| N_shots | n_tr | 保持 10% 以内线性到 | 此处 peak 均值 | 占硬上限 |
|---|---|---|---|---|
| 1 | 27 | boost 0.005 | 3.0 | 11.2% |
| 2 | 54 | boost 0.00475 | 6.0 | 11.0% |
| 4 | 108 | boost 0.0045 | 11.3 | 10.5% |

**② peak 封顶值到不了满 n_tr**（抖动让部分轨迹的雪崩错开 bin，无法在同一 bin 全部重合）：

| N_shots | 封顶 peak 均值 | 占硬上限 | 封顶起始 |
|---|---|---|---|
| 1 | 26.93 | 99.8% | boost ≈ 8 |
| 2 | 53.44 | 99.0% | boost ≈ 5 |
| 4 | 106.10 | 98.2% | boost ≈ 5 |

**③ peak 标准差是拱形，但拱顶不在二项分布预言的 p = 0.5，而在 0.28**

| N_shots | n_tr | 拱顶 σ | 二项峰值 √(n_tr/4) | 实测/二项 | 拱顶处占硬上限 |
|---|---|---|---|---|---|
| 1 | 27 | 1.90 | 2.598 | 0.731 | 0.280 |
| 2 | 54 | 2.70 | 3.674 | 0.735 | 0.279 |
| 4 | 108 | 3.80 | 5.196 | 0.731 | 0.279 |

三档 N 的拱顶占比一致为 **0.28**，实测/二项之比一致为 **0.73**，一致性好到可以直接引用。
原因：`peak` 是 **152 个 bin 上的最大值**，不是某个固定 bin 的计数。能量越大，被削平到硬上限附近的
bin 越多，它们互相竞争最大值，对涨落的压制越强，于是方差峰被推向更低的占比，整体幅度也被压低。

**④ FWHM 有 `T_OVER` 地板、永不饱和、且几乎与 N_shots 无关**

- 低能段 FWHM 稳定在 **8.2–8.4 ns**，等于 `T_OVER ≈ 8.00 ns`。
  **这一段量到的是器件过阈窗宽，不是激光脉冲宽度**——一次雪崩就点亮连续约 8 个 bin。
- peak 在 boost ≈ 20 封顶后，FWHM 仍按 **每十倍能量 +4.40 ns** 一直增长
  （三档 N 拟合值完全一致 4.40）。到 boost = 1e4 时 FWHM = 30.86 ns。
  机理：峰高被硬上限锁死后，脉冲前后沿被继续抬到半高线以上，过半高的时间跨度随能量的对数增长。
- N = 1 / 2 / 4 三条 FWHM 曲线基本重合：累加更多发只抬高峰、不改变波形相对形状。
  **正因为重合，必须用不同粗细 + 线型才看得出三条线**（`M5_STYLE_N`）。

### 踩过的坑

**坑 1：构建脚本的 code cell 里写了 `"""` docstring，把外层 `r"""` 提前截断**

- 错误做法：在 `code(r""" ... """)` 里给辅助函数写 `"""中文说明"""` docstring。
- 现象：`build_peak_vs_energy.py` **自身** `SyntaxError: invalid character '【'`；
  `.ipynb` 没被重写；紧接着跑的校验脚本用的是**旧 notebook**，图看起来「改了等于没改」，
  排查方向被完全带偏。
- 根因：cell 源码是用 `r"""..."""` 存的，内部任何 `"""` 都会终止它。
- 正确做法：cell 内的函数一律用 `#` 注释代替 docstring。
  另外给 `check_peak_vs_energy.py` 加了 **mtime 闸门**：构建脚本比 `.ipynb` 新就直接中止并提示，
  杜绝「校验跑在旧 notebook 上」。

**坑 2：PowerShell 用 `;` 串命令，前一条失败不会中断后一条**

- 错误做法：`python build.py > _b.txt 2>&1; python check.py`。
- 现象：构建失败被重定向进文件、没人看，校验照跑，配合坑 1 造成长时间误判。
- 正确做法：`python build.py; if ($LASTEXITCODE -eq 0) { python check.py }`，
  且构建输出**不要**重定向掉。

**坑 3：只设 `xlim` 不设 `ylim`，纵轴被视野外的数据撑开**

- 错误做法：低能段放大图只 `a.set_xlim(0, 0.005)`。
- 现象：matplotlib 的自动缩放依据的是**全部数据**，深饱和段的 peak=106 把纵轴顶到 0–108，
  低能段那段 0–11 的曲线被压成贴地的一条平线，完全看不出线性与否。
- 正确做法：模块 0 里的 `autoscale_y(a, series)`，按**当前 xlim 之内**的数据定纵轴；
  各画图 cell 在对应 `*_YLIM_* is None` 时调用它。

**坑 4：中文字体只装一个名字，缺字形没有回退 → 方框**

- 错误做法（项目里多个旧脚本都是这个写法）：
  `for _f in [...]: rcParams["font.sans-serif"] = [_f]; break`。
  这个 `try` 永远不会失败，等于只装了 `Microsoft YaHei` 一个，缺哪个字形就画成方框。
- 正确做法：直接给**列表** `["Microsoft YaHei", "SimHei", "DejaVu Sans"]`。
  matplotlib 3.11 支持按列表回退。校验脚本会打印实际命中的字体文件（现为 `msyh.ttc`）。

**坑 5：后台跑长任务不加 `-u`，stdout 块缓冲，看不到任何进度**

- 现象：任务跑了 80 s，终端文件里除了表头一片空白，无法判断是卡死还是正常。
- 正确做法：`python -u run_peak_energy_scan.py ...`。

**坑 6：peak 很低时 FWHM 无意义，且低能段的 FWHM 有选择偏倚**

- 现象：`boost = 5e-4` 时 peak 均值才 1.27，多数实现的 peak 是 0 或 1，半高只有 0.5 个计数。
- 做法：设 `FWHM_MIN_PEAK = 4` 门槛，并**同时记录有效样本占比**。
  模块 5 把有效占比 < 50% 的能量段涂灰并在图例里写「勿引用」——
  那一段的 FWHM 只由少数较亮的实现贡献，是有偏的。

**坑 7：密度条带不做列内归一化，色标被低能档拉满**

- 现象：低能档的概率几乎全压在 `peak = 0` 上（pmf ≈ 1），按全局概率上色会让其余所有列全黑。
- 做法：模块 2 默认 `M2_NORM_COLUMN = True`，每一竖列各自除以本列最大值，
  色条标注改为「列内归一化概率」。定量数值看叠加的均值线与模块 6 的表。

**坑 8（已避免，记录备查）：`fwhm` 若用「首个过半高 bin 到末个过半高 bin」会串峰**


- bg = 0 时信号是唯一计数来源、区间连续，用首/末尾没问题；
  但一旦把 bg 开成非零，远处的噪声尖峰会被算进来，宽度虚高。
- 实现上直接采用「**包含峰位的那一段连续过半高区间**」，为将来开 bg 留好余地。
  该实现已与朴素逐行实现在真实波形 + 6 种极端情形（宽平台、贴左/右边界、等高双峰、全零、窄峰）上
  **逐点对拍一致**。

---

## v01-c1（2026-08-10）**更正**：peak 标准差拱顶在 0.28 的成因，此前写错了

> 本条是对上面 v01 条目中「关键运行结论 ③」与「坑 9」的**更正**。
> 按规则，原条目不删改，以本条为准。

### 用户提出的质疑

> 「peak 的标准差是 0.28 处，我们说的是 noise 的标准差在 0.5 吧？这好像本来就不一样。」

**用户是对的。** `p = 0.5` 那条规律说的是**单个 bin 计数**的方差极大点，
而 `peak` 是另一个统计量。此前把 peak 的均值代进单 bin 的二项公式当参考、
再说实测「偏离」了它，等于立错了靶子。

### 此前写错的内容

原 v01 条目里写的是：

> 「原因：`peak` 是 152 个 bin 上的最大值……能量越大，被削平到硬上限附近的 bin 越多，
> 它们互相竞争最大值，对涨落的压制越强，于是方差峰被推向更低的占比。」

**这个归因是错的。**

### 分离验证（脚本 `check_singlebin_vs_peak.py`，输出 `check_singlebin_vs_peak_out.txt`）

把三个统计量分开测（N_shots=4，n_tr=108，bg=0，每档 6,000 次 MC，
「最亮 bin」按**该档自己的**平均波形定 —— 不能用最强档的，那时已是 30 ns 平台、峰位会漂移）：

| 统计量 | σ 峰值 | 出现在占比 |
|---|---|---|
| A　单 shot、单 bin 计数 | 1.876 | **0.277** |
| B　N=4 累加、单 bin 计数 | 3.777 | **0.278** |
| C　peak（152 bin 取最大） | 3.758 | **0.278** |

两条关键事实：

1. **B 与 C 几乎相等**（3.777 对 3.758）。bg = 0 时统计窗里只有一处有信号，
   **peak 实际就等于最亮 bin 的计数**，「取最大值」这一步几乎没起作用。
   所以原来的归因不成立。
2. **B/A 各档稳定为 2.00**（实测 1.983…2.054），即 4 个独立 shot 相加 ⇒ σ×2，符合预期。

### 正确的成因：`f_pix` 不均匀 ⇒ 泊松二项分布

信号在 27 个 SPAD 上的分配**极不均匀**：

- `f_pix` 最大 `0.006582`、最小 `2.731e-05`，相差 **240 倍**；
- 离散度 `std/mean = 1.05`。

于是各条轨迹的点亮概率 `p_t` 互不相同，单 bin 计数是**泊松二项分布**而非二项分布：

$$\sigma^2=\sum_t p_t(1-p_t)=n_{tr}\,\bar p(1-\bar p)-n_{tr}\operatorname{Var}_t(p_t)$$

右边第二项恒为正 ⇒ 方差**恒低于**二项，且方差极大值被推向更低的 `p̄`。

**定量佐证**：从实测方差反解隐含的 `Var_t(p_t)`，得 `√Var_t(p_t)/p̄` 在低能三档为
**0.98 / 1.01 / 0.95**，与 `f_pix` 的 `std/mean = 1.05` 吻合 ——
低能段 `p_t ∝ f_pix`，二者相对离散度本就应当相等。
能量升高后亮 SPAD 先饱和、离散度收缩，到 `boost ≈ 12.93` 时实测/纯二项 = **0.996**，完全回到二项。

实测/纯二项之比随能量的完整走向：0.943 → 0.875 → 0.807 → 0.728 → 0.651 → **0.586（最低）**
→ 0.596 → 0.621 → 0.797 → 0.923 → 0.996。

### 与纯噪声的口径区别（这正是用户问到的点）

**纯噪声时 27 个 SPAD 看到的环境光相同、`p_t` 全都一样**，才是严格二项，
方差极大值才在 `p̄ = 0.5`，即 PoD_esti 里的 `σ² = bg(1 − bg/n_tr)`。
**信号侧因为 `f_pix` 不均匀，本来就不该服从这条规律。两者口径不同，不可混用。**

### 落地改动

- `build_peak_vs_energy.py` 模块 4 的 markdown 整段重写：删去「取最大值造成」的错误归因，
  改为泊松二项解释 + 与纯噪声口径的对照。
- 模块 4 左图点线的图例由「二项参考」改为「**纯二项**（设各 SPAD 收集比例相同）」，避免再被当成应然值。
- 模块 4 的打印表增加 `f_pix` 离散度，并在表下补一段与纯噪声的对照说明。
- 新增常驻脚本 `check_singlebin_vs_peak.py` 与其输出 `check_singlebin_vs_peak_out.txt`。

### 新增的坑

**坑 10：不要拿单 bin 的方差规律去套 peak，也不要拿噪声的二项规律去套信号**

- 单 bin 计数与 peak 是两个统计量；纯噪声（`p_t` 全等）与信号（`p_t` 因 `f_pix` 差 240 倍而悬殊）
  也是两种分布。四者两两不可互推。
- 判断该用哪条规律，先问一句：**这 27 条轨迹的点亮概率是否相同？**
  相同 ⇒ 二项，极大在 0.5；不同 ⇒ 泊松二项，方差被 `n_tr·Var_t(p_t)` 压低、极大点左移。

**坑 11：定「最亮 bin」必须逐能量档各定各的**

- 错误做法：用最强能量档的平均波形 `argmax` 作为全局「最亮 bin」。
- 现象：最强档已是 30 ns 宽平台，`argmax` 落在 t = 119.5 ns（ToF 是 100 ns），
  对低能档而言那里几乎没有信号（占比 0.000–0.004），整张对照表全废。
- 正确做法：`j = argmax(该档自己的平均 hist_add)`。

---

## v01-c2（2026-08-10）判定实验：`f_pix` 均匀 vs 真实，确认成因；并更正拱顶位置的过度精确

> 本条进一步核实 v01-c1 的归因，并**更正 v01-c1 中「拱顶在 0.28」的过度精确表述**。

### 用户提出的第二次质疑

> 「不，我觉得你按光斑均匀做也还是一样。真正重要的原因难道不是，
> noise 的方差和 noise 有关，但是 peak 的方差是另一个衍生量的结果吗？」

即质疑两点：(a) `f_pix` 均匀化后拱顶不会回到 0.5；(b) 真正主因是「peak 是衍生量（取最大值）」。

### 判定实验（`check_uniform_vs_real_fpix.py`，输出 `check_uniform_vs_real_fpix_out.txt`）

同一套物理、同一个 `Σf_pix = 0.0597`、同样 n_tr=108 与 bg=0，只改分配方式，
28 档能量 × 6,000 次 MC，20 进程约 3.3 min：

- **REAL**：真实像斑轮廓，`f_pix` 最大 0.006582 / 最小 2.731e-05，相差 241 倍，`std/mean = 1.048`
- **UNIFORM**：27 个 SPAD 均分，每个 `f_pix = 0.002211`，`std/mean = 0`

| 配置 | 统计量 | 拱顶 σ | 拱顶处占比 |
|---|---|---|---|
| UNIFORM | 单 bin | 5.183 | **0.509** |
| UNIFORM | peak | 5.177 | **0.510** |
| REAL | 单 bin | 3.787 | 0.352 |
| REAL | peak | 3.778 | 0.353 |
| 纯二项理论 | — | 5.196 | 0.500 |

### 结论

**质疑 (a) 不成立。** 均匀化后拱顶回到 **0.509**、峰值 **5.183**，
与纯二项理论 0.500 / 5.196 吻合到 0.2%；UNIFORM 各能量档的「实测 σ / 二项 σ」
全部落在 **0.99–1.01**（严格二项，符合数学必然：各轨迹 p 相同 ⇒ Binomial(n_tr, p)）。
REAL 下该比值随能量走 0.98 → 0.87 → 0.73 → **0.58（最低）** → 0.75 → 0.91 → 1.00。
**`f_pix` 不均匀就是拱顶左移与幅度压低的成因，v01-c1 的归因成立。**

**质疑 (b) 部分成立，但不负责拱顶位置。**
拱顶附近，`peak 的 σ / 最亮单 bin 的 σ` 在两种配置下都是 **0.996–1.001**，取最大值几乎不起作用。
但该比值在**高占比区**确实显著偏离 1：

- REAL 占比 0.982 处 → **0.637**
- UNIFORM 占比 1.000 处 → **0.175**（所有 bin 都顶满，最大值几乎没有涨落）

所以「peak 是衍生量」这个效应真实存在，只是它主导的是**深饱和段**，不是拱顶位置。
两个机制各管一段。

### 更正 v01-c1 的过度精确表述

v01-c1 写「拱顶一致落在 0.28」「三档 N 一致，可直接引用」。**这两句都要收回**：

1. **拱很平。** 真实 `f_pix` 下，占比 0.22–0.38 之间 σ 只变化 3% 以内。
   argmax 的落点对 MC 噪声与能量网格都敏感：
   主缓存（20,000 MC）给 **0.28**，本次独立实验（6,000 MC）给 **0.35**，两者都在平台内。
   正确表述：「**拱顶在占比 0.2–0.4 这一段，明显低于纯二项的 0.5**」。
2. **三档 N 一致不是独立证据。** N=1/2/4 由**同一批 4 发实现**前缀和派生，随机性共享，
   数值一致是必然的，不能当作结论稳健的佐证。

### 附带发现：漫长的饱和过程也是 `f_pix` 不均匀的产物

- UNIFORM：占比 0.25 → 0.995 只需 boost 0.013 → 0.25（约 1.3 个数量级）。
- REAL：占比 0.25 → 0.97 拖到 boost 0.015 → 20（约 3 个数量级）。

主图里那段漫长压缩，源于暗 SPAD（`f_pix` 小到 2.7e-05）要等到很晚才被点亮。

### 落地改动

- 模块 4 markdown 重写：加入对照实验数据；明确「取最大值」只主导高占比区；
  明确拱顶应按区间引用；点明三档 N 共享随机性。
- 模块 4 左图新增 `M4_SHOW_PLATEAU` / `M4_PLATEAU_FRAC = 0.97`，
  用阴影标出 σ ≥ 峰值 97% 的平台段，免得 ★ 被当成精确位置。
- 模块 4 打印表末列改为「平台占比区间」，并注明两次独立实验给出 0.28 / 0.35。
- 新增常驻脚本 `check_uniform_vs_real_fpix.py` 与输出 `check_uniform_vs_real_fpix_out.txt`。

### 新增的坑

**坑 12：不要在平坦极值上报三位有效数字**

- σ 拱顶所在的一段占比 0.22–0.38 内 σ 只变 3%，argmax 基本是网格与随机数的函数。
- 报极值位置前，先看极值附近有多平、再对比一次独立种子/网格的结果。

**坑 13：共享随机性的多条曲线，一致不等于稳健**

- N=1/2/4 是同一批 4 发实现的前缀和，不是三次独立测量。
- 要做独立验证，必须换种子或换配置（如本次的 UNIFORM 对照）。

---

## v01-u（2026-08-10）主口径改为 `f_pix` 均匀照度，全量重算

### 起因

用户指令：「`@peak_vs_energy_v01.ipynb` 这个 改成 pixel 光强均匀分布。」
即把 v01-c2 判定实验里的 UNIFORM 配置扶正为 notebook 的主口径。

### 改动

**① `run_peak_energy_scan.py` 新增 `F_PIX_MODE` 开关**

- `"uniform"`（新默认）：27 个 SPAD 平分总收集比例，每个 `f_pix = 0.002211`，`std/mean = 0`。
  **Σf_pix = 0.05969 与真实像斑完全一致**，只改分配、不改总量。
- `"real"`：原来的真实像斑轮廓，保留可用。
- 可用环境变量临时切换：`set PVE_FPIX_MODE=real`。
- 缓存与检查点文件名带模式后缀（`peak_vs_energy_cache_uniform.npz` /
  `..._real.npz`），两份互不覆盖；`GRID_KEY` 前缀加 `fpix=<mode>|`。
- 缓存里新存 `f_pix_mode`（字符串）与 `f_pix`（27 元数组），notebook 直接读，
  不必再去 import 扫描脚本猜配置。

**② 两种模式各配一套能量网格**（`build_boost_grid(mode)`）

均匀照度下所有 SPAD 同步逼近饱和，`boost ≈ 0.3` 就封顶；真实像斑要等最暗的 SPAD 点亮，
拖到 `boost ≈ 20`。**饱和位置差近两个数量级，沿用旧网格会把大半机时浪费在已封顶的区间。**
uniform 网格 75 档：`0→0.005` 步长 2.5e-4（查线性）；`0.005→0.05` 步长 2.5e-3（σ 拱顶在此）；
`0.05→0.15` 步长 0.01；`0.15→0.5` 步长 0.05（封顶）；`0.5→1e4` 等比 19 档（只剩 FWHM 在长）。

**③ 已有真实光斑缓存的保全（遵循规则三：不许丢已算好的结果）**

原 `peak_vs_energy_cache.npz` 复制为 `peak_vs_energy_cache_real.npz`，
再用一次性脚本 `_migrate_real_cache.py` 给它补上 `f_pix_mode` 字段、把 `grid_key` 换成新格式。
迁移后 76/76 档仍然有效，**省掉一次 16 min 重算**。

**④ notebook 文本随口径改写**

- 首个 markdown 增加「像斑照度口径：`f_pix` 均匀分布」段落，写明总量不变、
  各轨迹 $p_t$ 相等、单 bin 服从二项分布，并指向真实像斑的对照缓存。
- 模块 0 打印当前模式、Σf_pix、每 SPAD 范围与离散度，并直说这意味着二项还是泊松二项。
- 模块 4 markdown 整段重写：均匀照度下**二项公式直接成立**，拱顶回到占比 0.5；
  真实像斑的泊松二项解释降级为「换成真实像斑会怎样」的对照说明。
- 模块 4 左图的纯二项参考线由细点线改成**压在底层的粗浅光晕**（`M4_BINOM_LW = 5.0`,
  `M4_BINOM_ALPHA = 0.28`）。均匀照度下实测与参考完全重合，同粗细两条线只看得到一条，
  必须靠「粗浅在下、细实在上」才看得出是两条。
- 横轴范围随饱和位置前移：`M2_XLIM_GLOBAL` / `M3_XLIM_GLOBAL` / `M4_XLIM_GLOBAL` /
  `M5_XLIM_GLOBAL` 由 `(0, 5.0)` 改为 `(0, 0.5)`。低能段与深饱和段的范围不变。
- 模块 5 markdown 里「`peak` 在 boost ≈ 20 封顶」改为「均匀照度 boost ≈ 0.3 封顶
  （真实像斑才是 20）」。

### 关键运行结论（uniform，75 档 × 20,000 MC，20 进程 15.2 min）

**① peak 标准差的拱顶回到二项预言值，且吻合得非常好**

| N_shots | n_tr | 拱顶 σ | 纯二项峰值 √(n_tr/4) | 实测/纯二项 | argmax 占比 | 平台占比区间 |
|---|---|---|---|---|---|---|
| 1 | 27 | 2.58 | 2.60 | **0.993** | 0.533 | 0.37 – 0.63 |
| 2 | 54 | 3.69 | 3.67 | **1.004** | 0.532 | 0.41 – 0.61 |
| 4 | 108 | 5.21 | 5.20 | **1.002** | 0.531 | 0.41 – 0.58 |

逐档核对（N=4，占比 0.05–0.95）「实测 σ / 二项 σ」全部落在 **0.987–1.005**，无系统性偏离。
对照真实像斑：同一比值最低压到 0.58，拱顶落在占比 0.2–0.4。
**这一条同时再次确认了 v01-c2 的归因。**

**② peak 封顶值几乎打满硬上限**

| 照度 | N=1 | N=2 | N=4 |
|---|---|---|---|
| uniform | 27.00（100.0%） | 54.00（100.0%） | 107.99（100.0%） |
| real（对照） | 26.93（99.8%） | 53.44（99.0%） | 106.10（98.2%） |

真实像斑封不满，是因为最暗的 SPAD 在有限能量下仍未必点亮；均匀照度没有这个短板。
三档 N 的封顶起点都在 `boost ≈ 0.13`（95% 判据）。

**③ 低能段线性区与真实像斑基本相同**

保持 10% 以内线性到 `boost ≈ 0.0075`（N=2/4）、`0.01`（N=1），
此处 peak 占硬上限约 18–23%。低能段 $p_t \approx \lambda f_t E$，
期望总计数只取决于 $\sum f_{pix}$，与怎么分配无关，所以两种模式在这一段本就应当重合。

**④ FWHM 结论不变**

- 地板仍是 `T_OVER ≈ 8 ns`（实测低能段 8.14–8.34 ns）。
- 深饱和段每十倍能量 **+4.37 ns**（三档 N 完全一致；真实像斑是 4.40 ns）。
- `boost = 1e4` 处 FWHM = **32.12 ns**（真实像斑 30.86 ns）。
- 三档 N 的 FWHM 曲线仍然基本重合，仍需靠 `M5_STYLE_N` 的粗细 + 线型区分。

### 踩过的坑

**坑 14：`np.load` 的 NpzFile 不关，`os.replace` 覆盖同名文件会被 Windows 拒绝**

- 错误做法：`z = np.load(path)` → 组装新字典 → 直接 `_save(R, path)` 覆盖原文件。
- 现象：`PermissionError: [WinError 5] 拒绝访问: '....npz.tmp.npz' -> '....npz'`。
  规则三要求的「先写临时文件再 `os.replace`」这一步被卡住。
- 根因：`NpzFile` 是惰性句柄，即使已经把所有数组取出来了，底层 zip 仍然开着；
  Windows 不允许替换被占用的文件（Linux 上不会报错，所以容易漏）。
- 正确做法：取完数组立刻 `z.close()`，再写回。

**坑 15：换物理配置时，能量网格必须跟着重新设计**

- 均匀照度的饱和点比真实像斑早近两个数量级。若沿用 real 网格，
  75 档里有 30 多档全落在「已经封顶、peak 一动不动」的区间，
  而拐点与 σ 拱顶所在的 `boost ∈ [0.005, 0.05]` 只剩零星几个点，曲线会画成折线。
- 做法：`build_boost_grid(mode)` 按模式分别给网格；网格变了 `GRID_KEY` 自然失配，
  不会误用旧缓存。

---

## v01-e（2026-08-10）**引擎 bug**：快速步进引擎在饱和时丢过阈覆盖，已修并全量重算

> 这一条推翻了 v01 / v01-u 里**所有饱和区**的数值。低占空区（远未饱和）不受影响。

### 用户提出的质疑

> 「你这个强信号的表现不对啊。完全没有封顶！！！和 `lidar_histogram_sim_v45.ipynb` 的表现完全不同。」

指的是模块 1 左图：boost 很大时平均累加波形应该压出一个贴着硬上限 `n_tr = 108` 的**削顶平台**，
实际却只到 90 上下，而且能量再大平台也不涨。

**用户是对的，这是引擎 bug，不是画图问题。**

### 根因

`pod_esti_v30_core.py` 里有两套引擎：

| 引擎 | 覆盖记法 |
|---|---|
| `spad_binary_trace`（精确，v45 cell 32 原样移植） | 对**每一次**雪崩执行 `out[(centers >= ta) & (centers < ta + T_OVER)] = 1`，取全部窗口的**并集** |
| `binary_macro_stepping(_per_shot)`（快速） | 只保留**最近一次**雪崩时刻 `tcov`，判据 `0 <= c - tcov < T_OVER` |

「最近一次雪崩」这个判据本身没错——数学上
`c 被点亮 ⟺ c 之前最近一次雪崩距 c 小于 T_OVER`。
**错在 `tcov` 未必是「c 之前」的那一次。** 饱和时 SPAD 在同一个 bin 内可以连续雪崩多次，
`tcov` 被改写成带 IRF 抖动（σ = 100 ps）的**更晚**时刻，于是 `c - tcov < 0`，
本该被前一次雪崩 8 ns 窗口点亮的 bin 被判成灭的。

关键量级：细网格步长 `dt = 200 ps`，抖动 `σ = 100 ps`，`T_OVER = 8.001 ns`，`τ_RC = 8.731 ns`。
深饱和时 SPAD 几乎每个细网格步都雪崩，`tcov` 恒等于「当前时刻 + 抖动」，
落在 bin 中心之后的概率约三成 ⇒ 占空被系统性压到 65–70%，**平台永远压不出来**。

### 修法

新增 `pod_esti_v30_core._cover_commit(c, tcov, cend, t_over)`，
把判据换成与精确引擎取并集**严格等价**的形式：

$$c\ \text{被点亮}\iff \max\{\,t_a + T_{OVER}\ :\ t_a \le c\,\} > c$$

bin 是按时间顺序出的，于是只要把「确实早于 `c` 的雪崩」并进单调不减的覆盖终点 `cend`，
再比一次 `cend > c`。`tcov > c`（抖动把它甩到 `c` 之后）的雪崩本轮不提交，留到下一个 bin 再算。
`binary_macro_stepping` 与 `binary_macro_stepping_per_shot` 都改了。

### 修复验证（`check_stepping_vs_exact.py`，60 次实现，N_shots=1，n_tr=27）

峰后 10 ns 平台值，快速 vs 精确：

| boost | 修复前（快速） | 修复后（快速） | 精确引擎 |
|---|---|---|---|
| 0.5 | 15.37 | 25.30 | 26.05 |
| 2 | — | 26.90 | 27.00 |
| 10 | **19.11** | **27.00** | **27.00** |
| 50 | — | 27.00 | 27.00 |

修复前 boost=10 处相对差 **29%**，修复后 **0.0%**。
低占空档（boost ≤ 0.05）两套引擎本来就在 MC 噪声以内，修复前后都一致，无系统性偏移。

### 重算后哪些数变了、哪些没变（uniform 口径，N_shots=4，硬上限 108）

| boost | 峰后 10 ns 平台（修复前 → 后） | peak 均值（前 → 后） | FWHM [ns]（前 → 后） |
|---|---|---|---|
| 0.5 | 70.56 → 70.66 | 107.66 → 108.00 | 11.17 → 12.76 |
| 11.41 | 70.11 → **107.98** | 107.99 → 108.00 | 19.29 → 19.29 |
| 1243 | 69.29 → **108.00** | 107.99 → 108.00 | 28.16 → 28.16 |
| 1e4 | 68.96 → **108.00** | 107.99 → 108.00 | 32.12 → 32.12 |

**变了的：平均波形的削顶平台。** 原来无论能量多大都卡在 69 左右（占硬上限 64%），
现在严格贴到 108，且平台宽度随能量单调展宽（`>107` 的 bin 数：boost=0.5 时 7 个，
2.4 时 11 个，11.4 时 14 个，1243 时 22 个，1e4 时 26 个）。这正是用户指出的「完全没有封顶」。

**没变的：peak 与 FWHM 的统计量。** 原因是这两个量都不依赖平台内部的取值：

- `peak` 取 152 个 bin 的最大值。旧引擎虽然把平台随机压低到约 65%，
  但总有若干 bin 侥幸没被压，最大值仍然打满 —— 所以 peak 均值只从 107.99 挪到 108.00，
  σ 拱顶（5.21，占比 0.531，实测/纯二项 1.002 → 1.003）几乎原地不动。
- `FWHM` 由**半高线与前后沿的交点**决定。旧引擎压低的是平台，
  平台值（约 70）仍远高于半高（54），交点位置几乎不动 —— 深饱和斜率仍是 4.37 ns/decade。

**结论：模块 1 的波形图此前是错的，模块 3/4/5/6 的定量结论不受影响。**
唯一有实质变化的表是模块 6 里 boost ≤ 0.5 一段的 FWHM（如 boost=0.5 由 11.17 变 12.76），
因为那里平台高度还没远离半高线。

### 连带更正：v01-c2 里「σ_peak/σ_单bin 在深饱和掉到 0.175」是 bug 的假象

用修好的引擎重跑 `check_uniform_vs_real_fpix.py`（28 档 × 6,000 MC）后：

| 项目 | 旧引擎（作废） | 修复后 |
|---|---|---|
| σ_peak/σ_单bin，UNIFORM 占比 1.000 | 0.175 | **0.981** |
| σ_peak/σ_单bin，REAL 占比 0.982 | 0.637 | **0.992** |
| σ_peak/σ_单bin，全 28 档两种照度 | 拱顶附近 0.996–1.001，高占比区大幅偏离 | **全程 0.978–1.001** |

**结论改写**：bg = 0 时「152 个 bin 取最大值」这一步**在任何能量下都不起作用**，
`peak` 恒等于最亮那个 bin 的计数。道理很直白 ——
没有环境光时统计窗里只有一处有信号、其余 bin 恒为 0，**没有第二个候选者来竞争最大值**。
旧引擎在饱和时把平台随机打穿，制造了本不存在的涨落差异，才让人以为有「深饱和段效应」。
**把 bg 开成非零之后这一条不再成立，届时必须重新评估。**

拱顶结论不受影响，重跑后与之前一致：

| 配置 | 统计量 | 拱顶 σ | 拱顶占比 |
|---|---|---|---|
| UNIFORM | 单 bin / peak | 5.183 / 5.177 | 0.509 / 0.510 |
| REAL | 单 bin / peak | 3.787 / 3.780 | 0.352 / 0.353 |
| 纯二项理论 | — | 5.196 | 0.500 |

### 缓存与波及范围

- `run_peak_energy_scan.py` 的 `GRID_KEY` 加了 **`ENGINE_TAG = "cov2"`** 前缀，
  修复前跑的缓存自动失配，不会被当成有效数据。**物理引擎再动，这个标签必须跟着加版本。**
- 修复前的两份缓存改名为 `peak_vs_energy_cache_{uniform,real}.buggy.npz` 留档（不要再引用）。
- **同样的 bug 存在于 `pod_esti_v05/v10/v11/v20_core.py`**（同一段代码历代复制）。
  本次只修了 v30，其余是历史版本，未动。
- **PoD_esti v30 的信号侧缓存受波及**（`run_pod_v30_sig_scan.py` 用的就是这个引擎）。
  阈值判决工作在**近门限、低占空**区，按上表该区两套引擎一致，结论大概率不变；
  但凡是引用到饱和区数值的结论都要重算复核。

### 踩过的坑

**坑 16：两套引擎并存却只对拍过低占空，等于没对拍**

- `theory_engine_equivalence.py` 的 T5b 只在**纯噪声/弱信号**下比过两套引擎，
  那里 SPAD 一个 bin 内基本不会雪崩两次，bug 根本不显形，于是一路带到 v30。
- 正确做法：等价性对拍必须**覆盖饱和区**。`check_stepping_vs_exact.py` 现在扫到 boost=50。
  精确引擎是逐光子 Python 循环，boost 再大就跑不动了（boost=1e4 时挂死），所以上界取 50。

**坑 17：判据写成「距最近一次雪崩的时间差」时，必须先保证那一次确实在 bin 之前**

- `0 <= c - tcov < T_OVER` 里的 `>= 0` 看着像在防「雪崩还没发生」，
  实际上一旦 `tcov` 因抖动越过 `c`，它就把**前一次**雪崩的有效覆盖一起吞掉了。
- 记时刻不如记**覆盖终点**：`cend` 单调不减，语义是「已确认覆盖到哪里」，不会出现这种吞掉。

**坑 18：Windows 上缓存文件被 Jupyter 内核占用，`os.replace` 会拒绝，长扫描的结果会丢**

- 现象：notebook 跑过一次后内核持有 `np.load` 的句柄，`Move-Item` / `os.replace` 一律
  `PermissionError: [WinError 5]`。15 min 的扫描在最后一步存盘失败。
- 正确做法：`_save()` 捕获 `PermissionError`，改写到 `<path>.new.npz` 并打印醒目提示。
  结果绝不能因为一个文件锁就丢掉。
