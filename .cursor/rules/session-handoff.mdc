---
description: 每次会话结束必须写/更新交接文档 handoff_<工作名>.md
alwaysApply: true
---

# 规则一：会话交接文档 `handoff_<工作名>.md`

**每一次会话结束前，必须写入或更新仓库根目录的 `handoff_<工作名>.md`。**

- **工作名 = 当前工作标识**，与 notebook / 工作名一致（例：工作 `PoD_esti` → `handoff_PoD_esti.md`；工作对应主产物 `PoD_esti_v05.ipynb` 时，工作名仍取 `PoD_esti`）。
- **禁止**使用 `handoff_现在工作.md` 或任何把「现在工作」四个汉字写进文件名的写法。
- 若该文档不存在 → 新建；若已存在 → 在原文档上修改、补充，不要推倒重写已有的有效信息。
- 读者假设：**一个完全没有上下文的新会话**。不要用只有本次会话才懂的简称、代号、"上面那个图"之类的指代。
  所有文件名、变量名、模块号、参数值都要写全。
- 文档必须包含以下小节（缺一不可）：
  1. **我们在做什么任务** —— 项目背景、当前工作名、目标产物是什么文件。
  2. **已经完成了什么** —— 按模块/版本列出，指明落在哪个文件的哪个 cell / 哪个函数。
  3. **目前面临的问题、卡在哪里** —— 现象、已排查到哪一步、还没验证的猜想。
  4. **下一步计划** —— 有序、可执行，写清"改哪个文件的哪个部分"。
  5. **踩过的坑（不要再踩）** —— 每条写清"错误做法 → 现象 → 正确做法/原因"。
- 交接文档只写事实与结论，不写过程流水账（流水账归 `worklog_<工作名>.md`）。
