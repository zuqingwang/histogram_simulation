# 交接文档：`peak_vs_energy`（信号能量扫描 —— peak 与半高全宽）

> 本文档假设读者是**一个完全没有上下文的新会话**。所有文件名、函数名、参数值均写全。
> 过程流水账在 `worklog_peak_vs_energy.md`，本文只写事实与结论。

> ⚠️ **2026-08-10 修了物理引擎的一个 bug**：`pod_esti_v30_core.binary_macro_stepping`
> 与 `binary_macro_stepping_per_shot` 在**饱和**时会丢掉过阈窗口的覆盖，导致强信号压不出
> 削顶平台（27 个 SPAD 的平台只到 19，精确引擎给 27）。修法见
> `pod_esti_v30_core._cover_commit`，验证见 `check_stepping_vs_exact.py`。
> **修复前产生的一切饱和区数值全部作废**；低占空区（远未饱和）不受影响。
> 缓存键已加 `ENGINE_TAG = "cov2"`，旧缓存会自动失配。
>
> 重算后：**模块 1 的波形图此前是错的**（深饱和平台卡在 69/108，现在严格贴到 108）；
> **模块 3/4/5/6 的定量结论不受影响**（peak 均值 107.99 → 108.00，σ 拱顶与 FWHM 斜率原地不动）。
> 原因是 peak 取 152 个 bin 的最大值、FWHM 由半高线与前后沿的交点决定，
> 两者都不看平台内部取值。详见 `worklog_peak_vs_energy.md` 的 v01-e 条目。

---

## 1. 我们在做什么任务

### 项目背景

仓库 `Histogram-simulation` 仿真的是一套 **TCSPC**（Time-Correlated Single Photon Counting，
时间相关单光子计数）激光雷达接收链路：**SPAD**（Single-Photon Avalanche Diode，单光子雪崩二极管）
组成的宏像元，对回波做 1 ns 分箱的**二值**采样，多发累加成直方图，再从直方图峰值判目标。

SPAD 是 **1 bit 器件**：一个 bin 内无论来多少光子，至多记 1。因此累加直方图存在**硬上限**，
强信号会被削顶，这是本工作全部反直觉现象的总根源。

### 当前工作名与目标

**工作名 `peak_vs_energy`**，目标产物 **`peak_vs_energy_v01.ipynb`**。

要回答的问题：**在没有环境光（bg = 0）的条件下，把信号能量从「平均不到一个光子」一路扫到「深度饱和」，
累加直方图的峰值计数 `peak` 与半高全宽 `FWHM` 如何变化**——包括它们的分布、均值和标准差。

起因是用户认为 `lidar_histogram_sim_v45.ipynb` 的**模块 18**（信号强度倍数扫描，
跨 7 个数量级、用 colorbar 串起波形演化）很好，希望把它从「看波形」推进到「看统计量」。

### 用户点名要的五张图（全部**线性坐标轴**）

| 编号 | 内容 | 落在 |
|---|---|---|
| 图 i（最重要） | peak 均值 vs 信号能量 | 模块 3 |
| 图 ii | peak 标准差 vs peak 均值 | 模块 4 左 |
| 图 iii | peak 标准差 vs 能量 | 模块 4 中 / 右 |
| 图 iv | 半高全宽 vs 能量 | 模块 5 左 / 中 |
| 图 v | 半高全宽 vs peak 均值 | 模块 5 右 |

### 口径定义（与 PoD_esti 全项目一致）

| 记号 | 含义 |
|---|---|
| `boost` | **信号能量倍率**，本工作的横轴。`boost = 1` 等于默认反射率 ρ=0.1 场景的回波强度。用户明确说过**单位不重要、只有比例有意义** |
| `bg` | `hist_add` 统计窗内每 bin 的平均计数。本工作固定 **bg = 0** |
| `hist_add` | N 发累加直方图，取值 0…`n_tr` |
| `n_tr` | 参与累加的轨迹数 = 27 SPAD × N_shots，即二值硬上限。N=1/2/4 → 27/54/108 |
| `peak` | `hist_add` 在统计窗 152 个 bin 内的最大计数 |
| `FWHM` | Full Width at Half Maximum，半高全宽 |
| `T_OVER` | 过阈窗宽 ≈ 8.00 ns。一次雪崩会点亮连续约 8 个 bin |
| `MC` | Monte Carlo，蒙特卡洛 |
| PDE | Photon Detection Efficiency，光子探测效率 |
| ToF | Time of Flight，飞行时间 |
| `f_pix` | 宏像元内每个 SPAD 的**空间收集比例**，27 个元素，和为 `Σf_pix = 0.05969` |

**`FWHM` 的精确口径（用户指定）**：对**单次实现的 `hist_add` 波形**测半高全宽，
取**包含峰位的那一段连续过半高区间**，两端线性插值到亚 bin（1 bin = 1 ns），
再对多次蒙卡求平均。`peak < 4` 计数的实现不计入。

**`f_pix` 的口径（2026-08-10 用户指定改为均匀）**：当前主口径是
**27 个 SPAD 照度完全相同**，每个 `f_pix = 0.002211`。总量 `Σf_pix` 与真实像斑一致，
**只改分配、不改总量**。于是同一 bin 里各条轨迹的点亮概率 `p_t` 严格相等，
单 bin 计数服从**标准二项分布** `B(n_tr, p)`，全部结论可直接与二项公式对照。
真实像斑轮廓（`f_pix` 最大 0.006582 / 最小 2.731e-05，相差 241 倍）的数据仍完整保留，
作为对照使用，见下节的 `F_PIX_MODE` 开关。

---

## 2. 已经完成了什么

### 文件清单与各自职责

| 文件 | 职责 |
|---|---|
| `pod_esti_v30_core.py` | 物理内核。提供引擎 `binary_macro_stepping_per_shot`、宏像元收集比例 `FPIX`、统计窗 `I_STAT0/I_STAT1`、`T_OVER` 等。**2026-08-10 由本工作修了一处饱和丢覆盖的 bug**（新增 `_cover_commit`），除此之外原样复用 |
| `check_stepping_vs_exact.py` | 两套引擎的等价性对拍，**覆盖到饱和区**（boost 0.002 → 50，两种 `f_pix`）。精确引擎是逐光子 Python 循环，boost 再大跑不动，所以上界取 50。约 1.5 min |
| `run_peak_energy_scan.py` | 离线蒙特卡洛扫描：多进程 + 落盘缓存 + 增量检查点。**唯一产生数据的地方**。顶部的 `F_PIX_MODE` 决定照度模式 |
| `peak_vs_energy_cache_uniform.npz` | **当前主口径**的扫描结果缓存，75 档，约 57 KB。存的是充分统计量，不是原始样本 |
| `peak_vs_energy_cache_real.npz` | 真实像斑轮廓的对照缓存，76 档，58 KB。已跑完，不要删 |
| `_migrate_real_cache.py` | 一次性迁移脚本，已执行完毕。给旧缓存补 `f_pix_mode` 字段、换新版 `grid_key`。留作记录，不需要再跑 |
| `build_peak_vs_energy.py` | 组装 `peak_vs_energy_v01.ipynb`。**要改 notebook 内容就改这里再重跑，不要直接编辑 `.ipynb`**，否则下次重建被覆盖 |
| `peak_vs_energy_v01.ipynb` | 主产物。只读缓存画图，**不做任何蒙特卡洛**，打开即可全跑 |
| `check_peak_vs_energy.py` | 无头校验：执行全部 code cell、每图存 `pve_*.png`、打印实际命中的字体文件 |
| `check_singlebin_vs_peak.py` | 专项验证：把「单 shot 单 bin 计数」「N=4 单 bin 计数」「peak」三个统计量分开测。输出存 `check_singlebin_vs_peak_out.txt`，约 3.5 min |
| `check_uniform_vs_real_fpix.py` | **判定实验**：真实 `f_pix` vs 均匀 `f_pix`（Σf_pix 相同）对照，证明 σ 拱顶左移源自 `f_pix` 不均匀而非「取最大值」。输出存 `check_uniform_vs_real_fpix_out.txt`，20 进程约 3.5 min |
| `worklog_peak_vs_energy.md` | 工作日志 |

### notebook 的结构

沿用 PoD_esti_v30 的三段式：
**模块说明（markdown，只留基本描述与缩写）→ 画图参数 cell（每个参数注明控制哪根轴）→ 画图 cell（只读缓存）**。

| 模块 | cell 号 | 内容 |
|---|---|---|
| 总说明 | 0 | 整体描述 + 缩写表 + FWHM 口径 |
| 0 | 1–2 | 载入缓存、派生统计量。定义了通用助手 `autoscale_y()` |
| 1 | 3–5 | 能量扫描总览。左＝平均累加波形（v45 模块 18 的 colorbar 画法）；右＝各自归一化到自身峰值 |
| 2 | 6–8 | peak 概率分布密度条带，3 列（N=1/2/4）× 2 行（低能段 / 全局） |
| 3 | 9–11 | **图 i**，三个横轴范围：低能段线性检验 / 全局 / 穿过饱和。附低能段线性度体检表 |
| 4 | 12–14 | **图 ii + iii**，含二项参考曲线、★ 拱顶标记、拱顶位置表 |
| 5 | 15–17 | **图 iv + v**，含 `T_OVER` 地板线、深饱和对数拟合、有效样本不足区灰色警示 |
| 6 | 18–19 | 全部 75 档的数值汇总表 + 结论速览 |

### 已跑完的数据

**当前主口径（uniform）**：75 个能量档 × 20,000 次 MC，20 进程 **15.2 min** 跑完，缓存已落盘。

| 配置项 | 取值 |
|---|---|
| `F_PIX_MODE` | `"uniform"` |
| `BG_SIG` | 0.0 |
| 能量网格 | `boost` 0 → 1e4（非零最小 2.5e-4），75 档，**非均匀采样** |
| `N_SHOTS_MAX` / `N_SHOTS_LIST` | 4 / (1, 2, 4) |
| 统计窗 | bin `[24, 176)`，152 个 |
| `FWHM_MIN_PEAK` | 4 |

**对照口径（real）**：76 档 × 20,000 MC，同样已跑完并落盘，可随时切回。

**照度模式怎么切**：改 `run_peak_energy_scan.py` 顶部的 `F_PIX_MODE`（`"uniform"` / `"real"`），
或临时用环境变量 `set PVE_FPIX_MODE=real`。
缓存文件名带模式后缀、`GRID_KEY` 前缀含 `fpix=<mode>|`，两份缓存互不覆盖，切回去不用重算。
notebook 读的是 `scan.CACHE`，跟着模式自动走；模块 0 会打印当前模式与 `f_pix` 离散度。

**两种模式的能量网格不同**，因为饱和位置差近两个数量级（uniform `boost ≈ 0.3` 封顶，
real 要拖到 `boost ≈ 20`）。见 `build_boost_grid(mode)`。

**一次 4 发仿真通过前缀和白拿 N=1/2/4 三档**，所以三档 N 几乎不额外花钱。

### 已验证的结论

以下数值全部来自**当前主口径 uniform**；括注「real」的是真实像斑对照缓存的数值。

**① peak 与能量在低能段严格正比，约在硬上限 20% 处离开线性**

| N_shots | n_tr | 保持 10% 以内线性到 | 此处 peak 均值 | 占硬上限 |
|---|---|---|---|---|
| 1 | 27 | boost 0.01 | 6.2 | 23.0% |
| 2 | 54 | boost 0.0075 | 9.5 | 17.7% |
| 4 | 108 | boost 0.0075 | 19.1 | 17.7% |

**这一段两种照度模式本就应当重合**：低能时 `p_t ≈ λ f_t E`，期望总计数 `= λ E Σf_pix`，
只取决于总量，与怎么分配无关。

**② peak 封顶值几乎打满 `n_tr`**

| 照度 | N=1 | N=2 | N=4 | 封顶起点（95% 判据）|
|---|---|---|---|---|
| **uniform** | 27.00（100.0%） | 54.00（100.0%） | 107.99（100.0%） | `boost ≈ 0.13` |
| real（对照） | 26.93（99.8%） | 53.44（99.0%） | 106.10（98.2%） | `boost ≈ 5–8` |

真实像斑封不满、且要拖到大三个数量级的能量，是因为最暗的 SPAD（`f_pix` 小到 2.7e-05）
很晚才被点亮。均匀照度下 27 个 SPAD 同步饱和，没有这个拖尾。

**③ peak 标准差是拱形，均匀照度下拱顶就在占硬上限 0.5，幅度等于 √(n_tr)/2**

| N_shots | n_tr | 拱顶 σ | 纯二项峰值 √(n_tr/4) | 实测/纯二项 | argmax 占比 | 平台占比区间（σ ≥ 97% 峰值）|
|---|---|---|---|---|---|---|
| 1 | 27 | 2.58 | 2.60 | **0.993** | 0.533 | 0.37 – 0.63 |
| 2 | 54 | 3.69 | 3.67 | **1.004** | 0.532 | 0.41 – 0.61 |
| 4 | 108 | 5.21 | 5.20 | **1.002** | 0.531 | 0.41 – 0.58 |

逐档核对（N=4，占比 0.05–0.95）「实测 σ / 二项 σ」全部落在 **0.987–1.005**，无系统性偏离。
模块 4 左图里实测曲线整条压在二项参考的粗浅光晕上。

**为什么成立**：照度均匀 ⇒ 各轨迹 `p_t` 相等 ⇒ 单 bin 计数是二项分布 `B(n_tr, p)` ⇒
`σ = √(n_tr·p(1−p))`，极大值 `√(n_tr)/2` 在 `p = 0.5`。
这与纯噪声侧 PoD_esti 用的 `σ² = bg(1 − bg/n_tr)` 是**同一条规律**，
因为两边现在都满足「各轨迹概率相同」这个前提。

**换成真实像斑就不成立**（对照缓存 `peak_vs_energy_cache_real.npz`）：
`f_pix` 最大 0.006582 / 最小 2.731e-05，相差 241 倍，离散度 `std/mean = 1.05`，
各轨迹 `p_t` 互不相同，单 bin 变成**泊松二项分布**：

$$\sigma^2=\sum_t p_t(1-p_t)=n_{tr}\,\bar p(1-\bar p)-n_{tr}\operatorname{Var}_t(p_t)$$

右边第二项恒为正 ⇒ 方差恒低于二项（比值最低压到 **0.58**），
拱顶被推到占比 **0.2–0.4** 那一段，幅度只有纯二项的约 0.73 倍。

**④ bg = 0 时，「peak 是 152 个 bin 的最大值」这一步自始至终不起作用**

判定实验 `check_uniform_vs_real_fpix.py` 直接比 `peak 的 σ / 最亮单 bin 的 σ`，
28 个能量档、两种照度模式，该比值**全部落在 0.978–1.001**。

原因很直白：没有环境光时统计窗里只有一处有信号，其余 bin 恒为 0，
**根本没有第二个候选者来竞争最大值**，于是 `peak` 恒等于最亮那个 bin 的计数。
**把 bg 开成非零之后这一条不再成立**，届时必须重新评估。

> **更正（2026-08-10）**：本节此前写过「高占比区该比值掉到 0.175（uniform）/ 0.637（real）」。
> 那是**引擎 bug 的假象**——旧引擎在饱和时把平台随机打穿，制造了本不存在的涨落差异。
> 用修好的引擎重跑后该现象完全消失。

**注意**：占比 > 0.95 之后不要再用「实测 σ / 由 peak 均值代入的二项 σ」这个比值判断，
因为那里 peak 均值已贴死 `n_tr`，代回二项公式分母趋于 0，比值失去意义。

**⑤ FWHM 有地板、永不饱和、几乎与 N_shots 无关**

- 低能段 FWHM 稳定在 **8.14–8.34 ns = `T_OVER`**。
  **这一段量到的是器件过阈窗宽，不是激光脉冲宽度。**
- peak 封顶后，FWHM 仍按 **每十倍能量 +4.37 ns** 增长（三档 N 拟合值都是 4.37；real 是 4.40），
  到 `boost = 1e4` 时 FWHM = **32.12 ns**（real 是 30.86 ns）。
  机理：峰高被硬上限锁死后，脉冲前后沿被继续抬到半高线以上，过半高的时间跨度随能量的对数增长。
- N = 1 / 2 / 4 的 FWHM 曲线基本重合：累加更多发只抬高峰、不改变波形相对形状。
  **正因为重合，必须靠 `M5_STYLE_N` 的不同粗细 + 线型才看得出三条线。**

### 已验证的实现正确性

`run_peak_energy_scan.fwhm_ns_batch()`（向量化）与朴素逐行实现，在**真实波形三档能量**
及**六种人造极端情形**（宽平台 20 bin、贴左边界、贴右边界、等高双峰、全零、窄峰 `[3,9,4]`）上
**逐点一致**。等高双峰只取含峰位那一段（返回 1.0 ns），符合设计意图。

无头校验 `check_peak_vs_energy.py` **全部 cell 通过**，实际命中字体 `C:\Windows\Fonts\msyh.ttc`，无缺字方框。

---

## 3. 目前面临的问题、卡在哪里

**没有阻塞性问题。**主流程已跑通、结论已验证。以下是已知的、非阻塞的遗留项：

0. **引擎修复的余波尚未清完**（2026-08-10，优先级最高）：
   - `check_uniform_vs_real_fpix.py` 与 `check_singlebin_vs_peak.py` 的已存输出是**旧引擎**测的。
     它们的拱顶结论在低占空区应当不变，但「占比 1.000 处 σ_peak/σ_单bin = 0.175」
     这类**深饱和**数值必须重跑复核。
   - `run_pod_v30_sig_scan.py` 用的是同一个引擎，**PoD_esti v30 的信号侧缓存受波及**。
     阈值判决工作在近门限、低占空区，按 `check_stepping_vs_exact.py` 的对拍该区两套引擎一致，
     结论大概率不变；但凡引用到饱和区数值的结论都要复核。
   - **同样的 bug 还在 `pod_esti_v05/v10/v11/v20_core.py` 里**（同一段代码历代复制）。
     本次只修了 v30，历史版本未动。要复现旧版结果时请注意这一点。

1. **uniform 网格里 `boost ∈ [0.15, 0.5]` 采样偏疏**（步长 0.05）。
   模块 2 的密度条带在这一段格子偏宽、有块状感。
   模块 3/4/5 的折线图不受影响（够光滑）。
   要加密就得改 `run_peak_energy_scan.build_boost_grid()` 的 uniform 分支，
   但**网格一变 `GRID_KEY` 就失配，必须全量重算**（约 15 min）。

2. **深饱和段 4.37 ns/decade 尚无解析推导**。
   直觉上应与脉冲双指数的 `τ_r = 0.7 ns` / `τ_f = 1.9 ns` 相关
   （朴素估计 `(τ_r+τ_f)·ln10 ≈ 6.0 ns/decade`，与实测 4.37 不符），
   差异推测来自「光子率 → 点亮轨迹比例」的非线性（`1 − e^{−μφ}`）以及 `T_OVER` 抹平，
   **尚未验证**。

3. **深饱和段在线性横轴上画不下**。跨 4 个数量级，线性轴必然压扁。
   现在的处理：模块 3/5 的「穿过饱和」面板默认铺到 `boost = 200`，
   完整数值以模块 6 的表为准；并各留了一个 `M3_USE_LOG_X_DEEP` / `M5_USE_LOG_X_DEEP` 开关
   （默认 `False`），需要时可切成对数横轴。

4. **未在 Jupyter 里 Restart & Run All**。无头校验已全过，缓存齐全，此项只是确认渲染排版。

---

## 4. 下一步计划

按优先级：

1. **在 Jupyter 打开 `peak_vs_energy_v01.ipynb` 执行 Restart & Run All**，确认图的排版与字体渲染。
   缓存齐全，全程不重算，预计几秒。

2. **若要切回真实像斑口径**：`set PVE_FPIX_MODE=real` 后重建 notebook 即可，
   缓存已存在、**不需要重算**。切回来前记得把模块 4 的 markdown 一并对照检查——
   现在那段文字是按均匀照度写的（二项成立、拱顶在 0.5），真实像斑下应改回泊松二项的表述。

3. **若要把 bg 开成非零**（看环境光如何抬高 FWHM 地板、如何移动 σ 拱顶）：
   改 `run_peak_energy_scan.py` 顶部的 `BG_SIG`，或把它改造成列表维度。
   注意 `GRID_KEY` 已含 `bg`，改了会自动判定缓存失配并重算，不会误用旧数据。
   `fwhm_ns_batch` 已按「包含峰位的连续过半高区间」实现，开 bg 后不会把远处噪声尖峰串进来。

4. **若要加密 `boost ∈ [0.15, 0.5]`**：改 `build_boost_grid()` uniform 分支的第 4 段，
   然后 `python -u run_peak_energy_scan.py --workers 20 --n-mc 20000` 全量重算（约 15 min）。

5. **若要推导 4.37 ns/decade 的解析式**：在 `pod_esti_v30_core` 的
   `signal_photon_rate_fine` 与 `binary_macro_stepping` 的 `1 − e^{−μφ}` 处入手，
   结论写进 `worklog_peak_vs_energy.md` 的历史记录。

### 改动流程（务必遵守）

```powershell
# 1. 改 build_peak_vs_energy.py（不要直接改 .ipynb）
# 2. 重建 —— 构建失败时不要往下走
python build_peak_vs_energy.py; if ($LASTEXITCODE -eq 0) { python -u check_peak_vs_energy.py }
```

---

## 5. 踩过的坑（不要再踩）

**坑 1：构建脚本的 code cell 里写 `"""` docstring，会把外层 `r"""` 提前截断**

- 错误做法：在 `build_peak_vs_energy.py` 的 `code(r""" ... """)` 里给辅助函数写 `"""中文说明"""`。
- 现象：`build_peak_vs_energy.py` **自身**报 `SyntaxError`，`.ipynb` 没被重写；
  接着跑的校验用的是**旧 notebook**，图「改了等于没改」，排查方向被完全带偏。
- 正确做法：cell 内的函数一律用 `#` 注释代替 docstring。
  `check_peak_vs_energy.py` 已加 **mtime 闸门**：构建脚本比 `.ipynb` 新就直接中止并提示。

**坑 2：PowerShell 用 `;` 串命令，前一条失败不会中断后一条**

- 错误做法：`python build.py > _b.txt 2>&1; python check.py`。
- 正确做法：`python build.py; if ($LASTEXITCODE -eq 0) { python check.py }`，
  且构建输出**不要**重定向掉。

**坑 3：只设 `xlim` 不设 `ylim`，纵轴会被视野外的数据撑开**

- 现象：低能段放大图只设 `set_xlim(0, 0.005)`，matplotlib 仍按**全部数据**自动缩放纵轴，
  深饱和段的 peak=106 把纵轴顶到 0–108，低能段曲线被压成贴地平线。
- 正确做法：用模块 0 定义的 `autoscale_y(a, series)`，按**当前 xlim 之内**的数据定纵轴。
  新增画图面板时记得调用它。

**坑 4：中文字体只装一个名字，缺字形没有回退 → 方框**

- 错误做法（本仓库多个旧脚本都是这个写法）：
  `for _f in [...]: rcParams["font.sans-serif"] = [_f]; break`。
  这个 `try` 永远不会失败，等于只装了 `Microsoft YaHei` 一个。
- 正确做法：直接给**列表** `["Microsoft YaHei", "SimHei", "DejaVu Sans"]`，matplotlib 3.11 支持按列表回退。

**坑 5：后台跑长任务不加 `-u`，stdout 块缓冲，看不到任何进度**

- 正确做法：`python -u run_peak_energy_scan.py ...`。

**坑 6：peak 很低时 FWHM 无意义，低能段的 FWHM 有选择偏倚**

- `boost = 5e-4` 时 peak 均值才 1.27，多数实现的 peak 是 0 或 1，半高只有 0.5 个计数。
- 做法：`FWHM_MIN_PEAK = 4` 门槛 + **同时记录有效样本占比**；
  模块 5 把有效占比 < 50% 的能量段涂灰并标「勿引用」。**引用低能段 FWHM 前先看有效占比。**

**坑 7：密度条带不做列内归一化，色标会被低能档拉满**

- 低能档概率几乎全压在 `peak = 0` 上（pmf ≈ 1），按全局概率上色会让其余所有列全黑。
- 做法：模块 2 默认 `M2_NORM_COLUMN = True`，每列各自除以本列最大值。

**坑 8：不要以为「线性坐标轴」等于「线性采样」**

- 用户要求五张图全部用线性坐标轴，但物理量跨 5 个数量级。
- 正确理解：要求针对**坐标轴**，不针对**采样点分布**。
  本工作采用「**采样非均匀、坐标轴线性**」，线性轴上曲线依旧光滑，也不浪费机时。

**坑 9：不要拿单 bin 的方差规律去套 peak，也不要拿噪声的二项规律去套信号**

- 这里有四个容易混淆的对象：**单 bin 计数** vs **peak（152 bin 取最大）**，
  以及**纯噪声**（各 SPAD 点亮概率相同）vs **信号**（各 SPAD 因 `f_pix` 相差 240 倍而悬殊）。
  四者两两不可互推。
- 判断该用哪条规律，先问一句：**这 27 条轨迹的点亮概率是否相同？**
  相同 ⇒ 二项分布，方差极大在 `p = 0.5`（当前 uniform 口径实测 0.53，实测/理论 0.99–1.00）；
  不同 ⇒ 泊松二项分布，方差被 `n_tr·Var_t(p_t)` 压低、极大点左移（real 口径实测 0.2–0.4）。
- **曾经踩过**：在 real 口径下，先以为 peak 的拱顶偏离 0.5 是「取最大值」造成的方差压缩。
  对照实验证否：均匀 `f_pix` 下拱顶回到 0.51，且 `peak σ / 单 bin σ` 全能量段都是 0.978–1.001。
  正确成因是 `f_pix` 不均匀（泊松二项）。详见 `check_uniform_vs_real_fpix.py` 与 worklog 的 v01-c1 / c2 / e 条目。
- **bg = 0 时「取最大值」在任何能量下都不起作用**：窗内只有一处有信号，没有第二个候选者。
  开了 bg 之后要重新评估。

**坑 11：不要在平坦极值上报三位有效数字，也别把共享随机性的曲线当独立证据**

- peak 的 σ 拱顶是一段平台：uniform 口径下占比 0.41–0.58 内 σ 只变 3%，
  real 口径下 0.22–0.38 内只变 3%。
  real 口径里 20,000 MC 给 argmax = 0.28，另一次 6,000 MC 的独立实验给 0.35，两者都在平台内。
  应表述为「拱顶在占比 0.2–0.4 这一段」。
- N=1/2/4 三档由**同一批 4 发实现**前缀和派生，随机性共享。
  三条曲线数值一致**不构成**独立验证，不要拿它当作结论稳健的依据。

**坑 12：定「最亮 bin」必须逐能量档各定各的**

- 错误做法：用最强能量档的平均波形 `argmax` 当作全局「最亮 bin」。
- 现象：最强档已是 30 ns 宽平台，`argmax` 落在 t = 119.5 ns（ToF 是 100 ns），
  对低能档而言那里几乎没有信号（占比 0.000–0.004），整张对照表全废。
- 正确做法：`j = argmax(该档自己的平均 hist_add)`。

**坑 13：`np.load` 的 `NpzFile` 不 `close()`，就没法用 `os.replace` 覆盖同名文件（Windows）**

- 错误做法：`z = np.load(path)` → 把数组都取出来 → 直接写回同一个 path。
- 现象：`PermissionError: [WinError 5] 拒绝访问: '....npz.tmp.npz' -> '....npz'`，
  规则三要求的「先写临时文件再 `os.replace`」这一步被卡死。
- 根因：`NpzFile` 是惰性句柄，取完数组底层 zip 仍开着；Windows 不允许替换被占用的文件。
  Linux 上不报错，所以很容易漏。
- 正确做法：取完数组立刻 `z.close()` 再写回。

**坑 14：换物理配置时，能量网格必须跟着重新设计，否则大半机时打水漂**

- 均匀照度的饱和点（`boost ≈ 0.3`）比真实像斑（`boost ≈ 20`）早近两个数量级。
- 若沿用 real 的网格：75 档里 30 多档全落在「已封顶、peak 一动不动」的区间，
  而拐点与 σ 拱顶所在的 `boost ∈ [0.005, 0.05]` 只剩零星几个点，曲线会画成折线。
- 做法：`build_boost_grid(mode)` 按模式分别给网格。网格变了 `GRID_KEY` 自然失配，
  不会误用旧缓存。

**坑 15：判据写成「距最近一次雪崩的时间差」时，必须先保证那一次确实发生在 bin 之前**

- 错误做法（v30 原实现，已修）：只记最近一次雪崩时刻 `tcov`，
  判据 `0 <= bin中心 - tcov < T_OVER`。
- 现象：饱和时 SPAD 在同一 bin 内连续雪崩，`tcov` 被改写成带 IRF 抖动（σ=100 ps）的更晚时刻，
  于是 `c - tcov < 0`，本该被**前一次**雪崩 8 ns 窗口点亮的 bin 被判成灭的。
  占空被系统性压到 65–70%，强信号**永远压不出削顶平台**。
  细网格步长只有 200 ps，抖动 100 ps，深饱和时几乎每步都雪崩，所以这个错判非常频繁。
- 正确做法：记**覆盖终点**而不是记时刻。`_cover_commit()` 维护单调不减的 `cend`，
  语义是「已确认覆盖到哪里」，判据 `cend > c`，与精确引擎取全部窗口并集严格等价。
- **教训**：`>= 0` 那个条件看着像在防「雪崩还没发生」，实际上它把前一次雪崩的有效覆盖一起吞掉了。

**坑 16：等价性对拍只覆盖低占空，等于没对拍**

- `theory_engine_equivalence.py` 的 T5b 只在纯噪声/弱信号下比过两套引擎。
  那里 SPAD 一个 bin 内基本不会雪崩两次，坑 15 的 bug 根本不显形，于是一路带到 v30。
- 正确做法：对拍必须**扫到饱和**。见 `check_stepping_vs_exact.py`。

**坑 17：`set PVE_FPIX_MODE=real` 会留在 PowerShell 会话里，之后重建的 notebook 会悄悄换成真实像斑**

- 现象：跑完 `real` 口径的扫描后，同一个终端里再 `python build_peak_vs_energy.py`，
  生成的 notebook 读的是 `peak_vs_energy_cache_real.npz`。
  图能正常出（σ 拱顶 3.8 / 占比 0.35），不报任何错，只是**口径悄悄换了**。
- 正确做法：临时切模式后，用完立刻 `Remove-Item Env:\PVE_FPIX_MODE`；
  或者干脆改 `run_peak_energy_scan.py` 里的 `F_PIX_MODE` 常量。
  校验时看模块 0 打印的 `像斑照度 f_pix = '...'` 一行确认口径。

**坑 18：Windows 上缓存文件被 Jupyter 内核占用，`os.replace` 会被拒，长扫描结果会丢**

- 现象：notebook 跑过一次后内核持有 `np.load` 的句柄，
  `Move-Item` / `os.replace` 一律 `PermissionError: [WinError 5]`；
  15 min 的扫描在最后一步存盘失败。
- 正确做法：`run_peak_energy_scan._save()` 已捕获 `PermissionError`，
  改写到 `<path>.new.npz` 并打印醒目提示。看到这个提示就关掉 Jupyter 内核再改名。
- 同理，`np.load` 取完数组要立刻 `close()`（见坑 13）。

**坑 19：两条曲线重合时，同粗细同风格只看得到一条**

- 现象：均匀照度下模块 4 左图的实测 σ 与纯二项参考完全重合，
  原来的「实线 + 细点线」画法看上去只有一条线，读者无从判断是重合还是漏画。
- 正确做法：参考线画**粗、浅、压在底层**（`M4_BINOM_LW = 5.0`、`M4_BINOM_ALPHA = 0.28`、
  `zorder = 1`）当光晕，实测细线盖在上面。同类问题在模块 5 用的是不同线型 + 粗细（`M5_STYLE_N`）。
