---
description: 每项工作从开工起必须维护 worklog_工作名.md
alwaysApply: true
---

# 规则二：工作日志 `worklog_<工作名>.md`

**从会话开始（开工那一刻）就要建立并持续维护 `worklog_<工作名>.md`**，工作名由用户给定
（例：工作名 `PoD_esti` → 文件名 `worklog_PoD_esti.md`）。

同一工作的交接文档为 **`handoff_<工作名>.md`**（例：`handoff_PoD_esti.md`），
与本日志共用同一工作名；**禁止**使用 `handoff_现在工作.md`。

文档固定分为**两部分**：

## 顶部：现状 / 当前任务
- 当前版本号、当前正在做的事、当前可运行状态（能跑通/跑不通、耗时）。
- 当前生效的关键参数取值。
- 待办清单（勾选框）。
- 这一段是**可覆盖的**，每次更新为最新现状。

## 下部：历史记录（只追加，不删改）
按版本倒序或顺序（全项目保持一致），每个版本记录：
- **新增**了什么（模块、函数、图）。
- **删减 / 停用**了什么，为什么。
- **改动**了哪些参数、由多少改到多少。
- **踩过的坑**：错误做法、现象、根因、修法。
- **运行结论 / 关键数值**。

历史记录条目不得回头删除或篡改；发现之前写错，另起一条写明"更正"。
