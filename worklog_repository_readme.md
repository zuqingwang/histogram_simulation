# 工作日志：`repository_readme`

> 顶部记录现状与当前任务；下部历史记录只追加，不删除旧条目。
> 交接文档见 `handoff_repository_readme.md`。

---

# 一、现状 / 当前任务

## 当前版本

**v01（2026-08-14）**

## 任务

为 `Histogram-simulation` 仓库编写根目录 `README.md`，并把同一版本同步到内部远端与 GitHub 镜像。
README 面向第一次接触项目的使用者，负责说明项目边界、信号链路、推荐入口、环境安装、常用命令、
缓存规则、验证方式、交接文档和已知限制。

## 当前状态

- `README.md` 已编写；
- 当前入口依据各工作线 handoff / worklog 核对，不按文件名猜测；
- 没有修改任何仿真代码、notebook、缓存或物理参数；
- README 的 23 个相对链接和 2 张预览图均已确认存在；
- 基础依赖导入、路径泄漏、Markdown 标题层级和 Git 空白检查均已通过；
- 本版本准备提交并同步到两个远端的 `master`。

## 关键决定

- README 使用中文，第一次出现的专业缩写同时给出英文全称和中文含义；
- 明确区分“直接打开缓存结果”和“重新运行耗时扫描”；
- 保留版本演进信息，提示历史文件不能按“看似旧”擅自删除；
- 明确 `crosstalk_sim_v42.ipynb` 没有对应 builder；
- 明确不同 peak 专题冻结在各自验证过的内核版本上，不能直接换成最新内核；
- 不写内部远端地址，只说明同一 `master` 在两个远端同步维护。

---

# 二、历史记录（只追加）

## v01 — 2026-08-14

### 新增

- 新增根目录 `README.md`；
- 新增项目数据流文本矢量示意图；
- 新增七个推荐入口及对应 handoff 索引；
- 新增 PowerShell 环境安装、notebook 重建、无头校验和长扫描示例；
- 新增缓存、复现、验证、已知限制和版本演进说明；
- 新增两张仓库内结果预览图。

### 验证

- README 相对链接：23 / 23 存在；
- 预览图片：2 / 2 存在；
- `numpy`、`scipy`、`matplotlib`、`nbformat`、`nbclient`、`nbconvert`、`openpyxl` 导入通过；
- 未发现本机绝对路径、内部远端地址或 `file://` 链接；
- `git diff --check` 通过。

### 删除

- 无。

### 参数改动

- 无。README 只描述已存在的参数口径和工作流。

### 关键结论

- 新用户应优先从各工作线“当前入口”读取结果，而不是直接运行扫描脚本；
- 生成型 notebook 必须从 builder 或分析源码修改；
- 历史版本、缓存和图像是复现链的一部分。

### 踩过的坑

- 不能把文件名中的最大版本号机械视作全仓通用最新内核；各专题有独立验证基线。
- `crosstalk_sim_v42.ipynb` 没有 builder，不能让 v41 构建器覆盖。
- 仓库没有依赖锁文件，README 只能列出基础包，不能声称已经固定可复现环境。
